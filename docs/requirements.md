# 个人记账 App 需求规格说明书 V3.1

---

## 1. 项目背景与产品定位

**背景**  
现有主流记账软件功能臃肿、启动慢、广告多，个人日常记账场景中大量功能未被使用。调研「随手记」后发现其核心体验良好，但启动广告和复杂功能令人困扰。

**产品定位**  
一款**极致轻量、零广告、纯本地、启动即记**的个人记账工具，专注为晚间回顾记账场景提供流畅的连续补记体验，同时兼顾消费后即时记录。支持固定周期自动记账，减少重复劳动。架构上预留联网与 AI 扩展点。

**核心价值**

- 打开即用，无广告干扰
    
- 晚间可连续补记多条流水，流程顺畅不中断
    
- 完全本地存储，零权限申请
    
- 灵活可配置的两级分类和自定义流水类型
    
- 多维度的流水筛选与三视图全能统计（构成分析、趋势分析、对比分析）
    
- 按月独立、智能沿用的预算管控，首页实时进度提醒
    
- 固定周期收支自动记录，减少手动重复记账
    
- 代码结构支持未来接入云同步与 AI 分析
    

---

## 2. 目标用户与使用场景

**用户画像**  
普通上班族，习惯在晚上花几分钟集中补记当日开销，偶尔也需在消费后即时记录。

**使用场景与权重**

|场景|占比|描述|核心需求|
|---|---|---|---|
|晚间回顾记账|75%|每天睡前花 3-5 分钟，回想当日消费，逐条补记。|连续快速添加多条记录，操作不中断，默认值合理，减少重复选择。|
|消费后即时记账|15%|买完咖啡、吃完午饭等消费后，立即掏出手机记录。|从打开 App 到保存完成路径极短，交互直接。|
|自动记账|10%|房租、订阅会员等固定收支，设置一次后自动记录。|设置简单，自动执行可靠，不重复记账。|

---

## 3. 功能性需求（MVP V1.0）

### 3.1 记一笔模块

|功能点|详细描述|
|---|---|
|流水类型|V1.0 仅内置「支出」和「收入」两种，不开放自定义（日常场景已足够）。切换流水类型时，分类列表自动切换为对应侧的支出分类或收入分类。预留接口供后续版本扩展。|
|金额输入|自动弹出数字键盘，支持小数点后两位。|
|分类选择|采用两级分类：一级大类 → 二级细项。分类按流水类型归属分两套：**支出侧**（餐饮、交通、购物等）和**收入侧**（工资、奖金、理财等），切换支出/收入时自动过滤。先展示一级分类网格，点击后展示二级细项网格；提供"跳过细项"选项，允许仅记录到一级分类。常用二级项在一级选中后自动置顶。|
|日期|默认当天，可手动修改。|
|备注|选填文本输入框。|
|连续记账模式|点击"保存"后，**不返回首页**，而是：①清空金额和备注；②保留已选的流水类型、分类、日期；③金额输入框重新获得焦点，键盘保持弹出；④顶部提供"保存并关闭"按钮，点击后返回首页。|
|快捷入口|在首页悬浮"+"按钮单击，直接进入记一笔页面，自动填充：流水类型=支出，日期=今天。|

### 3.2 分类管理模块

> **设计原则**：支出和收入的分类完全隔离，各自独立管理。分类管理页通过顶部 Tab 切换「支出分类」和「收入分类」，两套分类互不干扰。每个分类通过 `flowSign` 字段标记归属（-1=支出侧，1=收入侧）。

|功能点|详细描述|
|---|---|
|支出侧内置分类|一级：餐饮、交通、购物、娱乐、住房、通讯、医疗、教育、其他支出。每个一级下预设二级细项。内置分类**允许删除**。|
|收入侧内置分类|一级：工资、奖金、理财、兼职、报销、退款、红包、其他收入。每个一级下预设二级细项。内置分类**允许删除**。|
|自定义分类|用户可新增一级和二级分类，修改名称，更换图标，排序（拖拽）。新增时需指定归属（支出侧/收入侧）。|
|恢复默认|分类管理页提供"恢复默认分类"按钮，一键还原当前侧的全部内置分类（已存在的跳过，不重复添加）。|
|修改/删除规则|- 修改分类名称：仅影响后续选择，历史记录保留快照名称。  <br>- 删除分类：弹出三个选项：①直接删除（历史保留原分类快照名，ID 关联失效）②迁移至其他分类（选择目标分类，历史记录批量替换 ID 和快照名）③删除分类及所有关联记录。  <br>- 无分类时记一笔：弹出引导提示"还没有分类，去添加一个吧"，并自动跳转到分类管理页。|
|批量修改分类|提供「一键修改分类」工具，可按原分类名（含已删除分类）批量替换为新分类，支持按时间范围过滤，预览影响条目数后执行，操作放入一个数据库事务中。|

### 3.3 流水列表与详情

|功能点|详细描述|
|---|---|
|列表展示|每条显示：分类图标、一级/二级分类名、金额、备注（如有）、日期。自动生成的记录显示 🔄 标记。金额支出显示红色负数（如 -¥30.00），收入显示绿色正数（如 +¥5,000.00）。|
|默认分组|**按月分组**，同一个月内的记录按日期倒序排列（最新在上）。同一天内按创建时间倒序（后记的先显示）。分组标题显示"2026年6月"，并在右侧显示该月支出合计。|
|视图切换|列表顶部提供筛选栏，包含四个切换项：**日 / 周 / 月 / 年**。默认选中"月"。切换后列表按对应时间粒度分组，分组标题格式：日="6月7日 周六"，周="6月1日-6月7日"，月="2026年6月"，年="2026年"。  <br>年视图特殊处理：不直接平铺全年流水，而是显示每月合计摘要，点击某月可钻取到该月视图。切换粒度时，分类筛选状态保持。|
|分类筛选|筛选栏提供分类筛选入口，可选择"全部分类"或按一级分类单独查看。选中某分类后，筛选按钮显示当前分类名。与时间粒度和搜索可组合生效。|
|搜索|筛选栏下方提供搜索框，点击输入关键词，实时过滤列表。匹配范围：备注、一级分类名、二级分类名、金额。搜索框右侧有清除按钮。与时间粒度和分类筛选可组合使用。|
|空状态|无任何记录时显示"暂无记录"+"记一笔"按钮；筛选后无结果时显示"没有找到匹配的记录"+"清除筛选"按钮；搜索无结果时显示"未搜索到相关记录，换个关键词试试"。|
|入口默认状态|从首页概览卡片进入：月视图 + 全部分类；从首页汇总行进入：对应时间视图 + 全部分类；从统计页进入：保持统计页当前粒度 + 该分类筛选；独立进入：月视图 + 全部分类。|
|交互|点击条目进入详情页；**左滑条目**露出"删除"和"修改"按钮。|
|详情页|展示全部字段（只读），右上角有"编辑"按钮。点击后字段变为可编辑，底部出现"保存"按钮。若为自动生成记录，顶部显示提示条"此记录由固定模板「xxx」自动生成。编辑后将不再自动更新。"保存后 `recurringTemplateId` 置为 null。|
|删除确认|手动记录删除弹窗："确定删除此记录吗？"；自动记录删除弹窗："此记录由固定模板自动生成，删除后模板仍会在下次到期时生成新记录。确定删除？"|
|性能|列表采用分页加载（按月分段），Room 返回 Flow 配合手动分页或 PagingData。预加载相邻月份数据。年视图摘要用 SQL 聚合查询。支持至少 5 万条记录无卡顿。|

### 3.4 统计模块

#### 3.4.1 整体设计思路

统计模块回答用户的三个核心问题，对应**三个子视图**，用户左右滑动切换：

|子视图|回答的问题|主力图表|适用时间粒度|
|---|---|---|---|
|**构成分析**|钱花在哪些分类？|横向条形图（排名）|日/周/月/季/年|
|**趋势分析**|支出随时间怎么变化？|柱状图 + 折线图叠加|月/季/年|
|**对比分析**|这个月和上个月/去年同月比怎样？|双柱对比图|月/季/年|

#### 3.4.2 时间维度选择器（统一控制三个子视图）

- 位于统计页顶部，是三个子视图共享的总开关。
    
- 粒度切换方式：点击当前时间标签，弹出粒度选择菜单（日/周/月/季/年）。
    
- 各粒度下的显示与切换：
    

|粒度|默认值|显示格式|切换控件|
|---|---|---|---|
|按天|今天|"6月7日 周六"|左右箭头逐天切换；点击日期弹出日历|
|按周|本周|"6月1日 - 6月7日"|左右箭头逐周切换；点击弹出周选择器|
|按月|本月|"2026年6月"|左右箭头逐月切换；点击弹出年月选择器|
|按季|本季|"2026年Q2 (4-6月)"|左右箭头逐季切换；点击弹出季选择器|
|按年|今年|"2026年"|左右箭头逐年切换；点击弹出年份选择器|

- 三个子视图共享同一时间选择器，切换粒度时三个视图同步更新。
    
- 统计页顶部同时显示概览卡片：当前时间范围的支出总额、收入总额、预算剩余。
    

#### 3.4.3 子视图一：构成分析

**目的**：一眼看清当前时间范围内，钱主要花在哪些分类。

|区域|描述|
|---|---|
|支出/收入 Tab|顶部切换查看支出构成或收入构成。|
|横向条形图（主力）|各一级分类按金额从高到低排列，每个分类一个颜色，条形长度代表金额，右侧显示金额 + 占比%。底部显示合计。已删除分类用灰色默认图标。|
|二级分类明细|点击某条一级分类后，下方展开该分类的二级构成（同样是横向条形图，按金额从高到低）。再次点击同一分类收起。|
|流水跳转入口|点击"查看该分类全部流水 >"跳转到流水列表页，自动筛选为该分类 + 当前时间范围。|
|饼图切换按钮|右上角提供饼图切换按钮，用户可按需切换为饼图视图（辅助），饼图同样支持点击扇区展开二级明细。|

#### 3.4.4 子视图二：趋势分析

**目的**：看清支出随时间的变化趋势，发现异常月份，判断消费习惯是否改善。

**粒度与 X 轴数据点**：

|当前粒度|趋势图 X 轴|数据点数量|
|---|---|---|
|按天|过去 30 天，每天一根柱子|30 个点|
|按周|过去 12 周，每周一根柱子|12 个点|
|按月（默认）|过去 12 个月，每月一根柱子|12 个点|
|按季|过去 8 个季度，每季一根柱子|8 个点|
|按年|全部年份，每年一根柱子|全部|

|区域|描述|
|---|---|
|图表模式切换|三个按钮：纯柱状 / 纯折线 / 柱+折叠加。默认叠加模式。|
|图表区|显示过去 N 个时间单位的支出变化。预算线用虚线阶梯标注（每月预算可能不同）。超预算柱子用红色。|
|悬浮提示|点击某根柱子/数据点弹出该时间单位摘要（总支出、主要分类 TOP3）。|
|数据摘要|图表下方自动计算并显示：均值、最高月份及金额、最低月份及金额、环比变化。|

#### 3.4.5 子视图三：对比分析

**目的**：回答"这个月比上个月花得多了还是少了？花在哪了？"

|区域|描述|
|---|---|
|对比对象选择|支持切换：上月 / 去年同月 / 自定义月份。默认对比上月。按季看则对比上季/去年同季，按年看则对比去年。|
|总支出对比|两行数字对比，显示增长/下降百分比。增长红色，下降绿色。|
|分类对比双柱图|每个一级分类显示两根柱子（本月实色，对比月半透明），按本月金额从高到低排列。|
|增减明细列表|列出所有分类的增减金额和百分比，增长在前、下降在后。|
|二级明细对比|点击某个分类的对比条，展开该分类在两个时期的二级明细对比。|

#### 3.4.6 数据呈现原则

1. **颜色语义**：支出一律暖色系（不同分类不同色相），收入冷色系，超预算红色。
    
2. **排序原则**：所有分类排名图一律从高到低。
    
3. **数字精确**：金额显示到分（¥3,280.50），占比显示一位小数（32.5%）。支出金额显示红色负数（-¥30.00），收入显示绿色正数（+¥5,000.00）。
    
4. **空状态**：该时间范围无记录时，显示"暂无记账数据"并提供快捷记一笔入口。
    
5. **性能**：统计计算用 SQL 聚合（SUM + GROUP BY）在数据库层完成，不做内存遍历。
    

#### 3.4.7 技术实现要点

- 图表库：推荐 Vico（Compose 原生）或 MPAndroidChart
    
- 时间范围变化时，ViewModel 重新查询数据并更新 StateFlow，图表自动重组
    
- 按日/周/月/季/年的分组逻辑封装在 `GetStatisticsUseCase` 中
    
- 所有聚合查询尽量用 Room 的 `@Query` 在 SQL 层完成
    

### 3.5 预算模块

#### 3.5.1 概念说明

预算与结余是两个独立概念：

|概念|定义|计算方式|
|---|---|---|
|**结余**|账面上还剩多少钱|从记账日到现在的全部收入 - 全部支出|
|**预算**|本月计划花多少钱的上限|用户手动设定的金额目标|

#### 3.5.2 存储模型

预算**按月独立存储**于 budget 表，而非全局单一值。

|字段|类型|说明|
|---|---|---|
|year|Int|年份|
|month|Int|月份|
|totalBudget|BigDecimal?|月度总预算，null 表示该月无预算|
|isAutoGenerated|Boolean|是否由系统自动沿用生成（true=自动，false=用户手动设定）|

**核心规则**：

- 用户手动修改某月预算时，`isAutoGenerated = false`
    
- 未来月份或新月份自动沿用最近一次手动设定的值，标记 `isAutoGenerated = true`
    
- 统计时，每个月的预算取**该月实际存储的值**，历史数据不会因后续修改而改变
    

#### 3.5.3 默认行为

- 新用户首次使用：**不设预算**。首页预算进度条显示引导文字。
    
- 用户首次设置预算后：当月起生效，后续新月份自动沿用。
    
- 用户可随时将某月设为"无预算"（`totalBudget = null`）。
    

#### 3.5.4 修改预算行为

|操作|行为|
|---|---|
|修改当前月预算|更新当前月记录，`isAutoGenerated = false`。**不影响已过去的月份**。|
|修改未来月份预算|同上，仅更新该月。|
|修改过去月份预算|允许（事后补设或修正）。保存时轻提示"修改历史月份预算将影响该月统计"。|
|"仅本月生效"|默认行为，仅更新当前选中月份。|
|"本月起长期生效"|用户修改预算时弹窗提供此选项。选择后批量更新当前月及之后所有 `isAutoGenerated = true` 的月份为新值。手动设定过的月份保持不变。|

#### 3.5.5 预算功能在各页面的体现

|页面|体现|形式|
|---|---|---|
|**首页**|预算进度条|进度条 + "已花 ¥x / 预算 ¥x" + "剩余 ¥x"。正常绿色/蓝色，≥80% 橙色，超支红色。无预算时显示引导文字。点击跳转 F。|
|**首页**|预算设置入口|顶部栏右侧预算按钮，点击跳转 F。|
|**统计页**|预算剩余|顶部概览卡片与支出、收入并列显示。|
|**统计页**|趋势图预算线|虚线阶梯叠加，超预算月份柱子红色。|
|**统计页**|对比图预算执行率|两个对比月份的预算执行率数据。|
|**预算设置页**|预算配置管理|输入 + 历史列表。|

#### 3.5.6 预算设置页面

|区域|描述|
|---|---|
|月份切换|默认显示当前月。左右箭头可切换任意月份。|
|金额输入|数字键盘输入预算金额。为空时占位显示"未设置"。|
|"设为每月默认预算"勾选框|勾选后保存，将当前值批量应用到当前月及之后所有自动生成的月份。|
|保存按钮|点击保存，仅更新当前选中月份（除非勾选了默认预算）。|
|历史预算列表|下方展示各月预算记录。已过去月份标注"已执行"，默认锁定不可直接修改；点击后弹出确认提示，确认后方可编辑。未来月份标注"未执行"可正常编辑。自动沿用月份标注"自动沿用"。|

### 3.6 数据管理

|功能点|详细描述|
|---|---|
|导出 CSV|支持导出全部记录或按时间范围筛选后的记录，包含：流水类型、一级分类、二级分类、金额、备注、日期。文件通过系统分享菜单发送到公共存储（如下载目录）。|
|导入 CSV|选择 CSV 文件，校验格式，追加导入不覆盖。导入完成后反馈结果（成功 X 条 / 跳过 X 条 / 错误 X 条）。导入时自动创建不存在的分类。|
|数据存储|完全本地 Room 数据库，数据存放在应用内部存储。|
|清除所有数据|二次确认后删除所有数据（流水、分类、预算、模板），内置分类重新加载。|
|卸载行为|卸载 App 时，系统自动删除所有内部存储数据（含数据库），无残留。用户主动导出到公共存储的 CSV 文件不会被删除。|
|权限|V1.0 为零权限应用。|

### 3.7 固定收支自动记账

#### 3.7.1 典型场景

|场景|示例|周期|
|---|---|---|
|每月固定支出|房租、房贷、物业费、订阅会员|每月 X 日|
|每周固定支出|每周五聚餐、每周一交通充值|每周 X|
|每年固定支出|保险年费、域名续费、会员年费|每年 X 月 X 日|
|每日固定支出|每日早餐预算、通勤费|每天|

#### 3.7.2 触发与执行

- **触发时机**：每次 App 启动时检查一次（V1.0 不依赖后台服务）。
    
- **执行逻辑**：
    
    1. 查询所有启用的模板
        
    2. 对每个模板，检查 `nextExecutionDate ≤ 今天` 的应执行日期
        
    3. 对每个应执行日期，检查该日期是否已有同一模板的记录，无则自动创建一条收支记录
        
    4. 更新 `lastExecutedDate` 和 `nextExecutionDate`
        
- **补记策略**：若错过多个周期，一次性补记所有遗漏周期，最多补记过去 90 天内。**V1.0 限制**：自动记账仅在 App 启动时触发，不依赖后台服务。若用户连续超过 90 天未打开 App，超出部分将不会被补记。V1.1 计划通过通知提醒改善此问题。
    
- **防重复**：自动创建的记录关联 `recurringTemplateId`，执行前检查该日期是否已有同一模板的记录。
    
- **无效日期处理**：如每月 31 日，当月无 31 日时自动退到当月最后一天。
    

#### 3.7.3 与流水列表的联动

- 自动生成的记录在流水列表中显示 🔄 标记
    
- 编辑自动记录后，`recurringTemplateId` 置为 null，变为手动记录
    
- 删除自动记录时，提示模板仍会在下次到期时生成新记录
    


---

## 4. UI 布局与交互骨架

### 4.1 全局导航结构

底部导航栏在**首页 (A)、统计页 (C)、设置页**三个一级页面始终显示，在**记一笔页 (B)** 中隐藏。

|Tab|功能|
|---|---|
|首页|点击跳转到首页 (A)|
|统计|点击跳转到统计页 (C)|
|设置|点击跳转到设置页|

### 4.2 首页 (A)

**设计定位**：首页 = 本月快照 + 资产总览。所有数据固定为当前月（汇总区含今日/本周/本年），不提供时间切换。想看历史数据，去统计页。

**顶部栏**：

|元素|位置|功能|
|---|---|---|
|标题|左侧|静态文字"本月概览"。|
|预算按钮|右侧|点击跳转到预算设置页 (F)。|

**概览卡片（当前结余）**：

|元素|功能|
|---|---|
|当前结余|大字显示。全部收入 - 全部支出。正数正常色，负数红色。|
|总收入 / 总支出|小字显示。|
|点击行为|点击卡片跳转到流水列表页 (D)，默认月视图。|

**日/周/月/年汇总区**：

|元素|功能|
|---|---|
|四行固定汇总|今日 / 本周 / 本月 / 今年 的支出和收入。自动计算，无需用户选择。|
|点击行为|点击任意一行 → 跳转到流水列表页 (D)，自动筛选对应时间粒度。|

**预算进度条**：

|元素|功能|
|---|---|
|进度条|"已花 ¥x / 预算 ¥x" + "剩余 ¥x"。正常绿色/蓝色，≥80% 橙色，超支红色。|
|无预算|显示"设置月度预算 >"引导文字。|
|点击行为|点击跳转到预算设置页 (F)。|

**近 6 个月趋势图**：

|元素|功能|
|---|---|
|柱状图|显示近 6 个月（当前月往前推 6 个月）的月度支出。简洁版，不显示预算线。|
|点击行为|点击图表区域 → 跳转到统计页 (C) 趋势分析视图。|

**本月支出排行榜**：

|元素|功能|
|---|---|
|TOP 5 列表|按本月支出金额从高到低排列。每个分类显示名称、金额、横向比例条。|
|查看完整排行|点击 → 跳转到统计页 (C) 构成分析视图。|

**悬浮按钮**：

|元素|功能|
|---|---|
|(+) 按钮|右下角常驻。仅单击。打开记一笔页 (B)，默认流水类型=支出，日期=今天。|

### 4.3 记一笔页 (B)

|区域|元素|功能|
|---|---|---|
|顶部栏|关闭|不保存，返回上一页。|
||保存并关闭|保存后返回，退出连续记账模式。|
|金额区|金额显示|居中大字号，初始"0.00"。点击弹出数字键盘，支持小数点后两位。|
|流水类型|类型标签|水平可滚动：支出、收入、自定义类型。当前选中高亮。|
||自定义按钮|标签行末尾"+ 自定义"。点击跳转到分类管理页 (E)。|
|分类区|分类显示|显示当前选中分类名。未选择时显示"选择分类"。|
||点击行为|从底部弹出分类选择器（Bottom Sheet）。|
|分类选择器|取消|关闭选择器，不改变选择。|
||编辑|关闭选择器，跳转到分类管理页 (E)。|
||一级/二级网格|点击一级切换到二级，点击二级选中并关闭。含"跳过"选项。|
|日期与备注|日期|默认当天，点击弹出日期选择器。|
||备注|文本输入框，选填。|
|保存按钮|保存|底部固定。保存后清空金额和备注，保留类型和分类，金额框重新获焦。|

### 4.4 统计页 (C)

|区域|功能|
|---|---|
|时间选择器|居中显示当前时间范围。左右箭头切换；点击弹出粒度菜单（日/周/月/季/年）。|
|概览卡片|当前时间范围的支出总额、收入总额、预算剩余。|
|子视图 Tab|构成分析 / 趋势分析 / 对比分析。左右滑动或点击切换。|

**构成分析**：横向条形图（主力）+ 饼图切换（辅助），点击分类展开二级明细，底部可跳转流水列表。

**趋势分析**：柱+折叠加（默认），可切换纯柱状/纯折线。预算虚线阶梯标注，超预算红色。数据点点击弹出悬浮提示。下方显示均值、极值、环比。

**对比分析**：上月/去年同月/自定义对比。双柱图展示差异，增减红/绿标注。点击展开二级明细对比。

### 4.5 流水列表页 (D)

|区域|功能|
|---|---|
|筛选栏|[日] [周] [月] [年] 时间粒度切换 + [全部分类 ▼] 分类筛选。|
|搜索框|点击输入关键词，实时过滤。匹配备注、分类名、金额。与筛选可组合。|
|流水列表|按所选粒度分组，同天按创建时间倒序。条目显示分类图标、分类名、金额、备注、日期。自动记录带 🔄 标记。支出红色负数，收入绿色正数。|
|条目交互|点击进入详情页（只读），右上角"编辑"进入编辑模式；左滑露出删除和修改。|
|空状态|无记录/无筛选结果/无搜索结果分别显示对应提示和操作按钮。|
|年视图|摘要模式，点击某月钻取到月视图。|
|入口默认|从首页/统计/独立进入时分别设置默认视图和分类筛选状态。|

### 4.6 分类管理页 (E)

|功能|
|---|
|顶部 Tab：**支出分类** / **收入分类**，切换后列表独立。|
|"恢复默认分类"按钮，一键还原当前侧内置分类。|
|一级分类列表：图标、名称、二级数量。拖拽排序。左滑编辑/删除。点击展开二级。|
|二级分类列表：展开显示。拖拽排序。左滑编辑/删除。|
|删除确认：直接删除（保留记录）/ 迁移至其他分类 / 删除分类及所有关联记录。|
|新增按钮：选择归属侧（支出/收入）、一级/二级、输入名称、选择图标。|
|"一键修改分类"入口，批量替换分类名。|

### 4.7 预算设置页 (F)

|功能|
|---|
|月份切换：左右箭头切换任意月份。|
|金额输入：数字键盘。为空显示"未设置"。|
|"设为每月默认预算"勾选框。|
|保存按钮。修改历史月份时弹出确认提示。|
|历史列表：各月预算，标注"已执行/未执行/自动沿用"。|

### 4.8 固定收支模板页 (G)

**模板列表**：

|功能|
|---|
|"+ 新增模板"按钮。|
|已启用列表：卡片显示分类图标、名称、周期描述、金额、下次执行日期、启用开关。|
|已暂停列表：折叠显示，灰显。|
|模板卡片左滑：编辑/删除/暂停-启用。点击进入编辑。|
|删除确认：仅删除模板 / 删除模板及关联记录。|
|空状态："暂无固定模板，点击 + 新增"。|

**新增/编辑模板页**：

|字段|功能|
|---|---|
|流水类型|下拉：支出/收入/自定义。|
|金额|数字键盘。|
|分类|点击弹出分类选择器。|
|周期|下拉：每天/每周/每月/每年。|
|执行日|根据周期动态显示。|
|备注|选填。|
|启用开关|开启/暂停。|
|下次执行日期|自动计算，只读。|

### 4.9 设置页

|分组|项目|功能|
|---|---|---|
|数据管理|导出数据|选择时间范围，生成 CSV，分享。|
||导入数据|选择 CSV，校验，追加导入，反馈结果。|
||清除所有数据|二次确认后清空全部数据。|
|记账设置|分类管理|跳转 E。|
||预算设置|跳转 F。|
||固定收支模板|跳转 G。|
|显示设置|暗黑模式|V1.1（V1.0 显示灰色"即将推出"）。|
|关于|版本号|只读。|
||开源许可|查看许可证。|
||反馈与建议|打开邮件客户端。|

### 4.10 页面跳转关系总图

text

底部导航栏（常驻于 A、C、设置）
│
├── 首页 (A)
│   ├── 点击概览卡片 ──→ 流水列表 (D) 本月视图
│   ├── 点击汇总行 ──→ 流水列表 (D) 对应时间视图
│   ├── 点击预算进度条/预算按钮 ──→ 预算设置 (F)
│   ├── 点击趋势图 ──→ 统计页 (C) 趋势分析
│   ├── 点击排行榜/完整排行 ──→ 统计页 (C) 构成分析
│   └── 单击(+) ──→ 记一笔 (B)
│
├── 统计 (C)
│   ├── 点击分类条/查看流水 ──→ 流水列表 (D) 并自动筛选
│   └── 点击数据点 ──→ 悬浮提示（无跳转）
│
└── 设置页
    ├── 导出数据 → 时间范围选择 → 生成 CSV → 分享
    ├── 导入数据 → 文件选择器 → 校验 → 导入 → 结果反馈
    ├── 清除所有数据 → 二次确认 → 清空
    ├── 分类管理 → 跳转 E
    ├── 预算设置 → 跳转 F
    └── 固定收支模板 → 跳转 G
记一笔 (B)
├── 点击分类区 ──→ 分类选择器（Bottom Sheet）
│   └── 点击"编辑" ──→ 分类管理 (E)
├── 点击自定义流水类型 ──→ 分类管理 (E)
├── 点击日期 ──→ 日期选择器（弹窗）
├── 点击关闭 ──→ 返回 A
└── 点击保存并关闭 ──→ 保存后返回 A

---

## 5. 数据实体设计概要

> **注意**：所有金额字段统一使用 `BigDecimal` 类型，Room 通过 `TypeConverter` 实现 `String` 互转。所有金额计算禁止使用算术运算符，必须使用 `BigDecimal` 的精确运算方法。

### 5.1 收支记录表（transaction）

|字段|类型|说明|
|---|---|---|
|id|Long|自增主键|
|flowType|String|流水类型（支出/收入/自定义名称）|
|categoryL1Id|Long?|一级分类 ID，关联 `category_def.id`，删除分类后置 null|
|categoryL2Id|Long?|二级分类 ID，关联 `category_def.id`，删除分类后置 null|
|categoryL1|String|一级分类名快照（用于显示兜底）|
|categoryL2|String?|二级分类名快照（可选）|
|amount|BigDecimal|金额（正数，支出显示时加负号）|
|note|String?|备注|
|date|Long|交易日期（毫秒时间戳）|
|createTime|Long|记录创建时间|
|ledgerId|String|账本 ID，V1.0 固定为 "default"|
|recurringTemplateId|Long?|来源模板 ID，null 表示手动记录|

### 5.2 分类定义表（category_def）

|字段|类型|说明|
|---|---|---|
|id|Long|自增主键，唯一标识|
|name|String|分类名称|
|parentId|Long?|所属一级分类 ID，null 表示自身是一级分类|
|icon|String|图标标识|
|isDefault|Boolean|是否为内置分类|
|isHidden|Boolean|是否隐藏|
|sortOrder|Int|排序序号|
|flowSign|Int|流水类型归属：-1=支出侧，1=收入侧，0=通用（预留）。与 FlowType.sign 对齐，驱动记一笔页和分类管理页的分类过滤。|

### 5.3 预算表（budget）

|字段|类型|说明|
|---|---|---|
|year|Int|年份|
|month|Int|月份|
|totalBudget|BigDecimal?|月度总预算，null 表示无预算|
|isAutoGenerated|Boolean|是否自动沿用生成|
|createTime|Long|创建时间（毫秒时间戳）|
|updateTime|Long|最后修改时间（毫秒时间戳）|

### 5.4 周期模板表（recurring_template）

|字段|类型|说明|
|---|---|---|
|id|Long|自增主键|
|flowType|String|流水类型|
|categoryL1Id|Long?|一级分类 ID|
|categoryL2Id|Long?|二级分类 ID|
|amount|BigDecimal|金额|
|note|String?|备注|
|cycleType|String|DAILY / WEEKLY / MONTHLY / YEARLY|
|cycleValue|Long|周期值：DAILY=0；WEEKLY=1-7；MONTHLY=1-31；YEARLY=MMDD|
|isActive|Boolean|是否启用|
|lastExecutedDate|Long?|上次执行日期（毫秒时间戳）|
|nextExecutionDate|Long|下次执行日期（毫秒时间戳），预计算，每次执行后更新|
|createTime|Long|创建时间|

---

## 6. 非功能需求

|类别|要求|
|---|---|
|平台兼容|Android 7.0 (API 24) 及以上。|
|性能|冷启动 ≤ 1 秒；即时记账（从打开到保存完成）≤ 5 秒；连续记账单笔间隔 ≤ 8 秒。列表滚动保持 60fps，支持至少 5 万条记录。统计图表加载在 1 秒内。自动记账检查不增加启动耗时超过 200ms。|
|安装包大小|< 8MB。|
|权限|V1.0 为零权限应用。|
|安全|数据完全本地存储，不收集任何个人信息。卸载即清空全部数据。|
|可维护性|MVVM + Clean Architecture，分层清晰，依赖注入（Hilt）。|
|扩展性|数据仓库抽象为接口，预留 RemoteRepository 和 SyncManager 接口，数据库表预留 ledgerId 字段。为 AI 模块预留 AnalyticsEngine 接口。|

---

## 7. 扩展性设计（联网与 AI 预留）

### 7.1 架构预留

- **数据仓库接口**：`TransactionRepository` 为接口，当前实现为 `LocalTransactionRepositoryImpl`，未来可添加 `RemoteTransactionRepositoryImpl` 实现同步。
    
- **同步管理器**：`sync` 包中定义 `SyncManager` 接口，V1.0 为空实现。
    
- **用户账户占位**：`UserManager` 返回本地默认用户，后续可接入云登录。
    

### 7.2 AI 接入设想

- 预留 `AnalyticsEngine` 接口，V1.0 使用本地规则实现预算提醒，未来可替换为远程 AI 服务。
    
- 潜在 AI 功能：智能分类建议、自然语言记账（"午餐30元"）、消费习惯预测、异常消费提醒。
    
- 数据层面已预留完整字段，后期仅需网络层发送数据并处理回调，需用户授权与隐私合规。
    

### 7.3 多账本扩展

- 数据库已含 `ledgerId` 字段，V1.0 默认值 "default"，V1.2 版本将支持创建多个账本并切换。
    

---

## 8. 版本规划

|版本|核心内容|
|---|---|
|V1.0（MVP）|本地记账、支出/收入两套独立分类体系、连续记账、流水多维度筛选（日/周/月/年+分类+搜索）、三视图统计、按月独立预算（首页进度条+统计页联动）、固定收支自动记账（每天/每周/每月/每年）、CSV 导入/导出、批量修改分类、BigDecimal 精确计算|
|V1.1|暗黑模式、桌面小部件、按一级分类设预算、固定模板到期通知提醒、首页自定义布局、自定义流水类型（用户可新增如代付、转账等类型并指定归属侧）|
|V1.2|多账本支持、本地完整备份/恢复（JSON）、统计增强|
|V2.0|可选的云同步（Firebase 或自建），多设备登录|
|V3.0|AI 智能分类、自然语言记账、消费洞察建议|

---

## 9. 验收标准（V1.0）

1. 应用安装后不申请任何权限即可正常使用全部功能。
    
2. 中等配置手机冷启动时间 ≤ 1 秒。
    
3. 即时记账完整操作 ≤ 5 秒。
    
4. 连续补记单笔间隔 ≤ 8 秒。
    
5. 分类管理页提供"恢复默认分类"按钮，正确还原内置分类。
    
6. 删除分类时可选择三个选项：直接删除（保留记录）/ 迁移至其他分类 / 删除分类及记录。批量修改分类功能可正确预览并执行。
    
7. 无分类时记一笔弹出引导并跳转到分类管理页。
    
8. 流水列表支持日/周/月/年视图切换，年视图摘要模式；分类筛选和搜索可组合使用，切换粒度时分类筛选保持。空状态正确显示。自动记录显示 🔄 标记。
    
9. 统计页支持日/周/月/季/年时间粒度切换，三个子视图同步更新。
    
10. 构成分析：横向条形图按金额从高到低排列，支持展开二级分类和切换饼图。已删除分类用灰色图标显示。
    
11. 趋势分析：支持三种图表模式，预算虚线阶梯标注，超预算红色。
    
12. 对比分析：支持上月/去年同月/自定义对比，双柱图展示差异。
    
13. 预算按月独立存储，修改当前月不影响历史。首页进度条颜色和文字正确反映预算状态。
    
14. 固定模板支持每天/每周/每月/每年，启动时自动补记（最多 90 天），不重复生成。
    
15. 编辑自动生成的记录解除模板关联；删除自动记录弹出特定提示。
    
16. CSV 导入/导出字段齐全，校验正确，导入自动创建不存在的分类。
    
17. 所有金额使用 BigDecimal 精确计算，无浮点误差。
    
18. 至少 10,000 条记录下，列表滑动无卡顿，统计图表加载在 1 秒内。
    
19. 卸载 App 后重新安装，数据为空（确认无残留）。
    

---

此文档即日起冻结为 V1.0 开发的唯一依据。如需修改，将按版本号迭代记录。