# MyPurse 开发问题记录

> **文档版本**：V1.6 (2026-06-21)
> **最后更新**：2026-06-21
> **原则**：按版本分节，每个版本内问题编号从 1 开始。问题编号格式为"V1.0-#N"。

---

## 记录说明

每个问题标注发现日期、严重程度、所属模块。修复后追加 **修复** 字段。

**严重程度**：🔴 阻塞 / 🟠 功能缺陷 / 🟡 体验问题 / 🟢 优化建议

**修复字段格式**：`✅ 已修复（修复日期，会话 #N，方案一句话摘要）`。未修复则写 `🔲 未修复`。

**问题模板**：
```
### V1.0-#N：问题标题

- **发现日期**：YYYY-MM-DD
- **严重程度**：🔴/🟠/🟡/🟢
- **所属模块**：模块名
- **修复**：✅ 已修复（日期，会话 #N，方案摘要） 或 🔲 未修复

**现象**：
描述。

**根因分析**：
原因。

**解决方案**：
方案。

**涉及文件**：
- 文件：改动

**教训**：
教训。
```

---

## V1.0 阶段（2026-06-15 ~ 2026-06-20）

### 问题列表

### V1.0-#1：分类管理拖拽排序性能卡顿

- **发现日期**：2026-06-15
- **严重程度**：🟡 体验问题（PC 端已可感知，移动端预期更严重）
- **所属模块**：分类管理页 (E)

**现象**：
拖拽分类条目移动时，画面有明显卡顿，不够跟手。

**根因分析**：
1. `onDrag` 回调每帧（~60fps）都会调用 `onMoveCategory`，触发 LazyColumn 重组和逐条数据库写入
2. `animateDpAsState` 为每个列表项创建动画状态对象，增加重组开销
3. 每次拖拽过程中逐条执行 `updateSortOrder`，产生大量数据库 I/O

**解决方案**：
1. `onDrag` 中仅累积 `dragOffsetY`，不做任何数据变更；实际的位置交换逻辑移至 `onDragEnd` 执行
2. 将 `animateDpAsState` 替换为简单的条件判断（`if (isDragging) 8.dp else 0.dp`）
3. 新增 `batchUpdateSortOrder` DAO 方法，使用 `@Transaction` 在单次事务中批量更新所有受影响分类的排序序号

**涉及文件**：
- `CategoryManageScreen.kt`：拖拽手势逻辑重构
- `CategoryManageViewModel.kt`：`onMoveCategory` 改为调用批量更新
- `CategoryDefDao.kt`：新增 `batchUpdateSortOrder`
- `CategoryRepository.kt` / `CategoryRepositoryImpl.kt`：新增批量更新接口及实现

---

### V1.0-#2：拖拽排序后点击位置错乱

- **发现日期**：2026-06-15
- **严重程度**：🟠 功能缺陷
- **所属模块**：分类管理页 (E)

**现象**：
拖拽"餐饮"到新位置后，再次点击"餐饮"，响应的是原来位置（拖拽前的位置）的分类。

**根因分析**：
1. **闭包捕获了旧值**：`LazyColumn` 中 `itemsIndexed` 的 `pointerInput(category.id)` 的 key 未因重排序而改变，导致手势检测回调中捕获的 `index` 和 `categories` 仍是拖拽前的旧值
2. **UI 状态被覆盖**：`loadCategories()` 中的 `collect` 使用了 `_uiState.value.copy(...)` 而非 `current.copy(...)`，导致 `expandedParentId` 等 UI 状态字段在数据刷新时被重置为默认值

**解决方案**：
1. 在 `itemsIndexed` 的 lambda 中使用 `rememberUpdatedState` 包装 `index` 和 `categories`，确保长生命周期的 gesture 回调始终读取最新值
2. 将 `_uiState.value.copy(...)` 改为 `current.copy(...)`，保留所有 UI 状态字段

**涉及文件**：
- `CategoryManageScreen.kt`：添加 `rememberUpdatedState`，修复手势回调中的闭包捕获
- `CategoryManageViewModel.kt`：修复 `loadCategories()` 中的状态合并方式

**教训**：
- Compose 中 `pointerInput` 的 key 不变时，内部 lambda 捕获的变量不会自动更新，必须用 `rememberUpdatedState` 桥接
- `StateFlow.collect` 中使用 `value.copy()` 会丢弃 collect 之前的并发更新，应使用 `current.copy()`

---

### V1.0-#3："恢复默认"按钮无响应

- **发现日期**：2026-06-15
- **严重程度**：🟠 功能缺陷
- **所属模块**：分类管理页 (E)

**现象**：
1. 点击"恢复默认"按钮后，分类顺序未恢复，UI 无变化
2. 按钮仅恢复支出侧还是同时恢复收入侧不明确

**根因分析**：
1. `restoreDefaultCategories` 方法只处理了"添加缺失的一级分类"，完全忽略了：
   - 已存在分类的 `sortOrder` 恢复为默认值
   - 子分类的恢复
2. 子分类数据（`CategoryDefaults.subCategories`）从未被使用

**解决方案**：
重写 `restoreDefaultCategories` 方法，执行以下三步：
1. 将已存在的一级分类的 `sortOrder` 重置为默认值
2. 添加缺失的一级分类（含正确的 `sortOrder`）
3. 遍历 `CategoryDefaults.subCategories`，添加缺失的子分类

**涉及文件**：
- `CategoryRepositoryImpl.kt`：重写 `restoreDefaultCategories`

**待确认**：
- 用户提问"恢复默认是把收入和支出都恢复吗"——当前实现仅恢复当前选中的那一侧（支出或收入），通过顶部 Tab 切换后需分别点击恢复。后续可考虑增加"恢复全部"选项。

---

### V1.0-#4：分类模式下 LazyColumn 重复 Key 导致闪退

- **发现日期**：2026-06-16
- **严重程度**：🔴 阻塞
- **所属模块**：流水列表页 (D)

**现象**：
在流水列表底部工具栏，将分组方式从"时间"切换到"一级分类"或"二级分类"后，App 立即闪退。

**根因分析**：
`GroupedTransactionList` 中，LazyColumn 的 item key 始终使用 `group.rangeStart.toString()`。在分类模式下，所有 `TimeGroup` 的 `rangeStart` 都是同一个时间范围起始时间戳（如 `1718409600000`），导致多个 group header 产生相同的 key `"header_1718409600000"`，LazyColumn 检测到重复 key 后抛出异常崩溃。

**解决方案**：
根据分组模式区分 key 生成策略：
- 时间模式（`GroupMode.TIME`）：继续使用 `rangeStart.toString()`，因为每个时间段的起始时间天然唯一
- 分类模式（`CATEGORY_L1` / `CATEGORY_L2`）：改用 `group.label`（如"餐饮"、"交通"），因为分类名天然唯一

修改位置：`TransactionListScreen.kt` 的 `GroupedTransactionList` 函数，将 key 生成逻辑改为：
```kotlin
val groupKey = if (groupMode != GroupMode.TIME) group.label else group.rangeStart.toString()
```

**涉及文件**：
- `TransactionListScreen.kt`：修复 `GroupedTransactionList` 中的 `groupKey` 生成逻辑

**教训**：
- 当分组依据不是时间时，不能假设 `rangeStart` 唯一
- LazyColumn 的 `item(key = ...)` 的 key 必须在整个列表范围内唯一，重复 key 会导致崩溃而非警告

---

### V1.0-#5："StandaloneCoroutine was cancelled" 系统 Toast

- **发现日期**：2026-06-16
- **严重程度**：🟡 体验问题（仅 debug 构建出现，release 不受影响）
- **所属模块**：全局 / 流水列表页 (D)

**现象**：
操作过程中偶尔弹出 Toast 提示 "StandaloneCoroutine was cancelled"，不影响功能但观感不佳。

**根因分析**：
Android 14+ 系统在 debug 构建中会检测到 `CancellationException` 并通过 Toast 展示。本项目 `TransactionListViewModel` 中 `loadData()` 和 `searchData()` 使用 `loadJob?.cancel()` 取消上一个加载协程，当用户快速切换筛选条件时触发 cancel，系统将其作为"未捕获异常"弹出 Toast。

**解决方案**：
1. `TransactionListViewModel` 中：`cancel()` 前先注册 `invokeOnCompletion` 空回调吸收异常；协程内部显式 catch `CancellationException` 并重新抛出（遵循协程规范），与普通异常分开处理
2. `MyPurseApplication` 中：设置全局 `UncaughtExceptionHandler`，对 `CancellationException` 静默处理（仅打 log），其他异常仍走原有崩溃流程

**涉及文件**：
- `TransactionListViewModel.kt`：`loadData()`、`searchData()` 的 cancel 和异常处理逻辑
- `MyPurseApplication.kt`：新增全局未捕获异常处理器

**补充说明**：
正常的 release 应用不会出现此提示，因为该检测是 Android 系统仅在 debuggable=true 的构建中启用的调试行为。但即便如此，处理方案本身（全局异常处理器 + 区分 CancellationException）也是 Android 开发的最佳实践。

---

## 问题统计

| 严重程度 | 数量 | 已解决 |
|---|---|---|
| 🔴 阻塞 | 6 | 6 |
| 🟠 功能缺陷 | 9 | 9 |
| 🟡 体验问题 | 11 | 10 |
| 🟢 优化建议 | 2 | 1 |

---

### V1.0-#6：Room 版本升级导致已有数据丢失

- **发现日期**：2026-06-16
- **严重程度**：🔴 阻塞
- **所属模块**：数据层（AppDatabase）

**现象**：
添加预算功能后（数据库 version 从 2 升到 3），重新运行 App，之前添加的所有记录和分类自定义全部丢失。

**根因分析**：
`AppDatabase` 的 `fallbackToDestructiveMigration()` 策略在检测到版本号变化且无 Migration 时，会**直接删除旧数据库并重建**。数据库版本从 2→3 未提供对应的 Migration，触发了破坏性迁移。

**解决方案**：
1. 短期：为版本 2→3 添加正确的 Migration（`addMigrations(MIGRATION_2_3)`）
2. 长期原则：每次数据库 schema 变更都必须提供对应 Migration；`fallbackToDestructiveMigration()` 仅作为最后兜底

**涉及文件**：
- `AppDatabase.kt`：需补充 MIGRATION_2_3 和 MIGRATION_1_2

**教训**：
- `fallbackToDestructiveMigration()` 在开发期看似方便，但一旦有真实数据就会造成灾难性后果
- 每次加表/改字段都应写 Migration，不能依赖 destructive fallback
- 开发阶段最好也接入导出备份，减少数据丢失风险

---

### V1.0-#7：年视图无数据

- **发现日期**：2026-06-16
- **严重程度**：🟠 功能缺陷
- **所属模块**：流水列表页 (D)

**现象**：
年视图下数据为空，即使当年有大量记录。

**根因分析**：
`calculateTimeRange` 中年视图的 `rangeEnd` 计算使用了 `Calendar.set(DECEMBER, 31)`，在 lenient 模式下 Calendar 会自动进位导致年份回退，实际计算的是错误的时间范围。

**解决方案**：
显式设置 YEAR、MONTH、DAY_OF_MONTH 三个字段，避免 lenient 模式自动调整。

**涉及文件**：
- `TransactionListViewModel.kt`

---

### V1.0-#8：粒度切换后时间状态残留

- **发现日期**：2026-06-16
- **严重程度**：🟡 体验问题
- **所属模块**：流水列表页 (D)

**现象**：
切换时间粒度（如从月切换到周）后，显示的不是当前周而是上一个粒度对应的时间。

**根因分析**：
`setGranularity()` 切换粒度时沿用了上一粒度的 `currentTime` 状态，未重置为当前时间。

**解决方案**：
`setGranularity()` 切换粒度时重置 `currentTime` 为 `System.currentTimeMillis()`。

**涉及文件**：
- `TransactionListViewModel.kt`

---

### V1.0-#9：月粒度趋势图导航到历史时间时显示错误数据

- **发现日期**：2026-06-16
- **严重程度**：🟠 功能缺陷
- **所属模块**：统计页趋势图 (C)

**现象**：
在趋势图中将粒度切换为"月"，然后导航到 2022 年（或其他历史年份），图表没有显示"无数据"，而是错误地显示了 2025 年（数据库中最早有数据的年份）的数据。

**根因分析**：
月粒度趋势查询 `getMonthlyExpenseTrendOnce` 的 SQL 只有下限 `date >= :sinceTimestamp`，缺少上限 `date <= rangeEnd`。其他粒度（日/周/季度）都正确使用了 `BETWEEN rangeStart AND rangeEnd`，唯独月粒度遗漏了上限条件。

当用户导航到 2022 年 3 月时，`rangeStart = 2021-04-01`，SQL 查出来的是 `>= 2021-04-01` 的所有数据。因为数据库中最早的数据是 2025 年的，图表就显示了 2025 年的数据，而不是期望的空结果。

**解决方案**：
1. DAO 层：`getMonthlyExpenseTrendOnce(sinceTimestamp)` 改为 `getMonthlyExpenseTrendOnce(rangeStart, rangeEnd)`，SQL 从 `WHERE date >= :sinceTimestamp` 改为 `WHERE date BETWEEN :rangeStart AND :rangeEnd`
2. Repository 层：调用时传入 `rangeEnd` 参数

**涉及文件**：
- `TransactionDao.kt`：DAO 方法签名和 SQL
- `TransactionRepositoryImpl.kt`：调用处补传 rangeEnd

**教训**：
- 时间范围查询必须同时设置上下限，单边条件在大时间跨度下会产生静默错误（不崩溃但数据错误，更难发现）
- 同类粒度的查询应保持一致的 SQL 模板，不一致往往是 bug 的信号

---

### V1.0-#10：BudgetEntity 架构回退（按月存储 → 单条记录）

- **发现日期**：2026-06-16
- **严重程度**：🟠 功能缺陷
- **所属模块**：预算功能 (F)

**现象**：
最初预算设计使用 `year + month` 复合主键（每个月的预算独立存储）。实际使用中发现该方案存在严重问题：首页展示时无法确定取哪个预算值、用户需每月重新设置预算、跨月查询逻辑复杂。

**解决方案**：
将 BudgetEntity 从按月存储简化为单条记录（`id=1` 唯一记录）。budget 表始终只有一行，修改即覆盖，无历史追踪。对应调整了 BudgetDao 的查询/写入方法。

**涉及文件**：
- `BudgetEntity.kt`：字段从 year+month+amount 简化为 id+amount
- `BudgetDao.kt`：查询改为 `WHERE id = 1`，写入用 `OnConflictStrategy.REPLACE`

**教训**：
- 个人记账场景下，按月预算的复杂度收益不成比例
- 过度设计（预先假设需要历史预算追踪）增加了不必要的复杂度

---

### V1.0-#11：周日周范围计算边界错误

- **发现日期**：2026-06-16
- **严重程度**：🟡 体验问题
- **所属模块**：流水列表页 (D) / 首页 (A)

**现象**：
周日时，本周范围（周一至周日）计算错误。`Calendar.SUNDAY = 1`，`Calendar.MONDAY = 2`，直接用 `dayOfWeek - Calendar.MONDAY` 计算时周日会得到 `-1`，导致本周起始日期计算到错误的日期。

**解决方案**：
对周日做特殊处理：`val daysFromMonday = if (dayOfWeek == Calendar.SUNDAY) 6 else dayOfWeek - Calendar.MONDAY`。周日时向前推 6 天到周一。

**涉及文件**：
- `TransactionRepositoryImpl.kt`：`getWeeklySummary` 中的周范围计算

**教训**：
- `Calendar` 的周日=1、周一=2 的常量定义容易造成直觉偏差
- 边界条件（一周的起始和结束日）需要显式测试

---

### V1.0-#12：收入聚合用 `!= '支出'` 而非 `= '收入'`

- **发现日期**：2026-06-16
- **严重程度**：🟡 体验问题
- **所属模块**：数据层 (TransactionDao)

**现象**：
余额查询和汇总查询中，收入聚合使用 `WHERE flow_type != '支出'` 而非 `WHERE flow_type = '收入'`。正常设计应直接用等值匹配。

**根因分析**：
系统中可能存在或未来可能引入非标准的正向流水类型（如"退款"、"转账收入"等）。使用 `= '收入'` 会遗漏这些类型的金额，导致余额和汇总数值偏小。`!= '支出'` 是兜底策略——所有非支出都视为收入。

**当前状态**：
该写法是有意为之的防御性设计，当前功能正常。但语义上不够明确，如果将来引入第三种类型（如"内部转账"既不增也不减），当前写法会错误地将其计入收入。

**涉及文件**：
- `TransactionDao.kt`：`getBalance`、`getDaySummary`、`getRangeSummary` 等查询

---

### V1.0-#13：`fallbackToDestructiveMigration` 破坏性迁移

- **发现日期**：2026-06-16
- **严重程度**：🔴 阻塞（已记录为 #6，此处为补充）
- **所属模块**：数据层 (DatabaseModule)

**现象**：
`DatabaseModule.kt` 中数据库构建使用了 `fallbackToDestructiveMigration()`，注释标记 `TODO: 阶段 5 替换为正式 Migration`。当前每次 schema 变更且无 Migration 时，数据库被删除重建，用户数据全部丢失。

**当前状态**：✅ 已解决（2026-06-17）

**解决方案**：
1. 在 `AppDatabase.kt` companion object 中新增三个 Migration：
   - `MIGRATION_1_2`：`ALTER TABLE category_def ADD COLUMN flow_sign`
   - `MIGRATION_2_3`：`CREATE TABLE budget`
   - `MIGRATION_3_4`：`CREATE TABLE recurring_template`
2. `DatabaseModule.kt` 中移除 `.fallbackToDestructiveMigration()`，改为 `.addMigrations(*AppDatabase.ALL_MIGRATIONS)`

**涉及文件**：
- `AppDatabase.kt`：新增 Migration 定义
- `DatabaseModule.kt`：替换构建策略

---

### V1.0-#14：WheelPicker 滚轮选择器高亮条与选中文字错位

- **发现日期**：2026-06-17
- **严重程度**：🟡 体验问题
- **所属模块**：日期选择器组件（ChineseDatePickerDialog）

**现象**：
1. 年月滚轮选择器（`YearMonthWheelSheet`）中，灰色高亮条与蓝色选中文字错位一行——高亮条在某项上，蓝色文字在相邻的另一项上
2. 流水列表页的日期选择器在打开时闪退

**根因分析**：
1. **`spacerCount` 值错误**：LazyColumn 结构为 `[顶部spacer(80dp)] + [items列表] + [底部spacer(80dp)]`，只有一个顶部 spacer item，但 `spacerCount` 被设为 `2`，导致所有 LazyColumn 全局索引 → items 数组索引的偏移计算差了一行
2. **空列表 `coerceIn` 崩溃**：流水列表的 `availableYears` 是异步加载的，初始状态 `yearList` 为空列表 `[]`（非 null）。`items.size - 1` 计算得 `-1`，`coerceIn(0, -1)` 抛出 `IllegalArgumentException`
3. **`snapshotFlow` 延迟**：基于 `snapshotFlow` 的居中项检测在快速滚动时有帧延迟，改用 `derivedStateOf` 实现实时计算

**解决方案**：
1. 将 `spacerCount` 从 `2` 修正为 `1`，并调整所有索引计算公式：
   - `initialFirstVisibleIndex = spacerCount + selectedIndex - visibleItems/2`
   - `animateScrollToItem((index + spacerCount - visibleItems/2).coerceAtLeast(0))`
   - 居中项计算：`itemsIdx = nearestIdx - spacerCount`
2. 将 `snapshotFlow` + `collect` 替换为 `derivedStateOf`，在组合阶段同步计算居中项
3. 为 `WheelPicker` 添加 `if (items.isEmpty()) return` 空列表防护
4. `resolvedYearList` 中 `yearList?.sortedDescending()` 改为 `yearList?.takeIf { it.isNotEmpty() }?.sortedDescending()`，过滤空列表

**涉及文件**：
- `ChineseDatePickerDialog.kt`：修复 `spacerCount`、`derivedStateOf` 重构、空列表防护、`resolvedYearList` 过滤

**教训**：
- 涉及 LazyColumn 索引映射时，必须精确数清楚前面有几个 spacer/header item，写成常量并加注释说明
- 任何涉及"偏移量"的计算（列表索引、像素位置、数组下标），写完一定要手动验算几个边界值（第一项、中间项、最后项）
- `snapshotFlow` 有延迟，需要帧级同步的场景应使用 `derivedStateOf`
- 异步加载的列表数据在初始状态可能为空（非 null），`coerceIn(0, items.lastIndex)` 在 `items` 为空时 `lastIndex` 为 `-1`，会导致崩溃。应始终先判空

**举一反三**：
- 统计页的 `YearMonthSheet` 中有 `WheelPickerForStat`（`WheelPicker` 的副本），如果存在类似的 `spacerCount` 错误，也应一并修复
- 记一笔（`AddTransactionScreen`）和流水列表（`TransactionListScreen`）共用同一个 `ChineseDatePickerDialog`，但传入的 `yearList` 参数不同（前者传 null 自动生成 ±30 年，后者从数据库异步统计有数据的年份），修改时需保证两种场景都正常

---

### V1.0-#15：流水列表日期选择器点击年月应弹出独立页面

- **发现日期**：2026-06-17
- **严重程度**：🟡 体验问题
- **所属模块**：流水列表页 (D) / 日期选择器组件（ChineseDatePickerDialog）

**现象**：
原设计中，点击日期选择器中的"2025年6月"标签后，年月选择区域在日历上方内联展开，占用日历空间，视觉上不美观。

**解决方案**：
新建独立的 `YearMonthWheelSheet` 组件，点击年月标签时弹出 ModalBottomSheet，内含年、月两个滚轮选择器 + 取消/确认按钮。与日历区域解耦，交互更清晰。

**涉及文件**：
- `ChineseDatePickerDialog.kt`：新增 `YearMonthWheelSheet` 组件，修改年月标签点击行为

---

### V1.0-#16：编辑流水记录页日历未中文化 + 代码重复

- **发现日期**：2026-06-17
- **严重程度**：🟠 功能缺陷（日历未中文化）+ 🟡 架构问题（代码重复）
- **所属模块**：流水列表页 (D) / 编辑详情页

**现象**：
1. 流水列表中点击单条记录进入编辑页，日期选择器显示的是 Material3 原生英文日历（Jan/Feb/Sun/Mon），而非中文日历
2. `TransactionDetailScreen` 独立实现了一套编辑 UI（流水类型选择、金额输入、备注、日期选择），与 `AddTransactionScreen` 功能高度重叠，且功能有缺失（不能修改分类）

**根因分析**：
- `TransactionDetailScreen` 第 153 行使用了 `DatePickerDialog`（Material3 原生），而记一笔和流水列表用的是 `ChineseDatePickerDialog`
- 编辑页被设计为独立页面而非复用记一笔，导致分类选择器、金额键盘等全部重新实现了一遍

**解决方案**：
1. 删除 `TransactionDetailScreen`、`TransactionDetailViewModel`、`TransactionDetailUiState` 三个文件
2. `AddTransactionScreen` 增加 `transactionId` 参数，支持编辑模式
3. 编辑模式下：标题改为"编辑记录"、隐藏"保存并继续"按钮、显示模板解除关联警告
4. 编辑导航从 `Route.TransactionDetail(id)` 改为 `Route.AddTransaction(transactionId = id)`
5. 日期选择器自然继承 `ChineseDatePickerDialog`，日历中文化问题自动修复

**涉及文件**：
- 删除：`TransactionDetailScreen.kt`、`TransactionDetailViewModel.kt`、`TransactionDetailUiState.kt`
- 修改：`AddTransactionScreen.kt`、`AddTransactionViewModel.kt`、`AddTransactionUiState.kt`、`Route.kt`、`MyPurseNavHost.kt`

**教训**：
- 遇到"编辑/新建"这类功能时，优先考虑复用而非新建。编辑本质上是"预填数据的新建"，应在同一页面通过模式切换实现
- 同一个功能（日期选择）出现在多处时，必须确保使用同一组件，避免每处独立实现导致行为不一致
- 举一反三：`RecurringTemplateEditScreen` 也可能存在类似问题（编辑模板 vs 新建模板），应检查是否需要合并

---

### V1.0-#17：统计页 WheelPickerForStat 是公共 WheelPicker 的复制品

- **发现日期**：2026-06-17
- **严重程度**：🟡 体验问题（功能正常但存在维护风险：公共 WheelPicker 修 bug 时统计页副本不会同步）
- **所属模块**：统计页 (C) / 公共组件

**现象**：
统计页 `YearMonthSheet` 中使用的 `WheelPickerForStat` 与 `ChineseDatePickerDialog.kt` 中的 `WheelPicker` 功能完全一致，但实现有差异：
- 公共版使用 `derivedStateOf`（同步），副本使用 `snapshotFlow`（有帧延迟）
- 公共版有 `spacerCount` 偏移量计算和空列表防护，副本没有
- 公共版修了多次 bug 后，副本仍停留在初始实现状态

**根因分析**：
统计页的时间选择器是独立开发的一套，没有复用 `ChineseDatePickerDialog` 中已有的滚轮组件。这是"同功能同组件"原则缺失的典型表现。

**解决方案**：
1. 公共 `WheelPicker` 从 `private` 改为 `internal`，新增 4 个颜色参数（`highlightColor`、`selectedTextColor`、`unselectedTextColor`、`backgroundColor`）
2. 删除统计页 `WheelPickerForStat`（~100 行），改用公共 `WheelPicker`，传入统计页配色参数
3. 统计页滚轮同时获得了公共版的改进：无帧延迟、正确偏移量、空列表防护

**涉及文件**：
- `ui/components/ChineseDatePickerDialog.kt`：`WheelPicker` 可见性提升 + 参数化颜色
- `ui/statistics/StatisticsScreen.kt`：删除 `WheelPickerForStat`，改用公共 `WheelPicker`

**教训**：
- 组件复用不通过参数控制差异，就会导致"复制一份改改"——这是技术债务
- 扫描确认：项目中还有年份选择器重复（统计页 3 处 + 流水列表 1 处各自实现），待后续处理

---

### V1.0-#18：Git 回退操作导致 UI 代码大量丢失

- **发现日期**：2026-06-17
- **严重程度**：🔴 阻塞（代码层面：设置页回到占位版、路由被还原、已删除文件被恢复）
- **所属模块**：全局（Git 操作失误 + 缺乏防护机制）

**现象**：
1. 会话 #14 执行 `git checkout` 回退操作后，多个 UI 文件被还原到旧版本
2. `SettingsScreen.kt` 从完整的 4 分组页面退回到占位版（只有"设置"标题）
3. `Route.kt` 被还原到 HEAD，丢失了 `AddTransaction` 的 `transactionId` 参数
4. 已删除的 `TransactionDetailScreen.kt` 等 3 个文件被恢复
5. 颜色体系在回退过程中出现偏差——部分文件是会话 #14 的新配色，部分是被还原的旧配色，导致视觉不一致

**根因分析**：
1. **直接原因**：`git checkout` 操作覆盖了工作区中未提交的改动。项目自 `8dddcf0` 提交后，阶段 7/8/9 的 25 个修改文件 + 6 个删除文件 + 14 个新增文件从未提交过 git，`checkout` 命令将这些未追踪的改动全部丢弃
2. **深层原因 — 缺乏提交节奏**：多个阶段的大量改动集中在一个工作区中，中间没有提交 checkpoint。一旦出错，丢失的是数天的工作成果
3. **深层原因 — 缺乏回退前的安全检查**：执行 `git checkout` 前没有确认 `git status` 中哪些文件会被影响，也没有做备份（`git stash` 或手动复制）
4. **深层原因 — 文档状态与代码状态不同步**：开发日志记录了会话 #7~#9 的所有改动，但 `git checkout` 回退后，代码状态与会话记录产生了偏差。后续会话只能根据文档记录手动重建，但重建过程中引入了新的不一致（如通配符导入、颜色值微调等）

**修复过程**：
会话 #16 耗时整个会话手动修正回退偏差：
1. `SettingsScreen.kt`：按设计文档重建（4 分组、数据管理含清除、显示设置灰显、关于含反馈与建议）
2. `Route.kt`：补回 `AddTransaction` 的 `transactionId` 参数，删除 `TransactionDetail` 路由
3. `MyPurseNavHost.kt`：移除 RecurringTemplate/TransactionDetail 的 composable 注册，修正编辑跳转

**但修复后仍有残留问题**：
- `SettingsScreen.kt` 通配符导入 `import icons.filled.*` 导致 IDE 警告（后续修复为显式导入）
- 颜色风格与会话 #14 之前有微妙差异（`surfaceVariant`、`outlineVariant`、`windowBackground` 等被显式赋值）
- 部分回退偏差可能尚未被发现

**涉及文件**：
- `ui/settings/SettingsScreen.kt`：完全重建
- `ui/navigation/Route.kt`：补回丢失的改动
- `ui/navigation/MyPurseNavHost.kt`：移除过期路由注册
- `ui/theme/Color.kt`、`ui/theme/Theme.kt`、`ui/theme/AppColors.kt`：颜色体系存在版本不一致

**教训**：
- **Git 是唯一的防线**：在 AI Agent 阶段式开发模式下，多个独立会话的改动共享同一个工作区。如果没有定期提交，一次 git 误操作就会丢失跨会话的累积成果。这是比单人开发更脆弱的状态
- **提交粒度应与会话边界对齐**：每个阶段/会话完成后应立即 commit，形成 checkpoint。不攒多个阶段再一起提交
- **`git checkout -- <file>` 的风险被严重低估**：当前规范 5.3 节标注为"低"风险，但这是错误的——在工作区有大量未提交改动时，`checkout --` 同样不可逆。规范应修正
- **回退操作前必须做快照**：执行任何可能丢失改动的 git 操作前，应先用 `git stash` 或 `git diff > backup.patch` 保存当前状态
- **文档不能替代 git**：开发日志虽然记录了改动内容，但无法替代代码本身。手动从文档重建代码必然引入偏差（如通配符导入、颜色值差异等）
- **颜色/主题改动应在独立 commit 中完成**：颜色体系从 1 层扩展到 3 层（`Color.kt` → `AppColors.kt` → 页面级色值），涉及多个文件联动。如果在回退中这些文件的版本不一致，就会出现"颜色看起来不对"的问题

**举一反三**：
- 项目中所有未提交的改动（25 个修改文件 + 6 个删除 + 14 个新增）都处于风险中，应尽快提交
- 规范 5.3 节的 `git checkout --` 风险等级应从"低"改为"中高"，并增加前置检查步骤
- 应在开发流程中增加"阶段完成 → 立即 commit"的强制要求

---

### V1.0-#19：统计页导航重构时底部栏隐藏逻辑被误删

- **发现日期**：2026-06-20
- **严重程度**：🟠 功能缺陷
- **所属模块**：导航 (MyPurseNavHost)

**现象**：
将统计页从底部导航 Tab 移除后，通过首页 TopAppBar 统计图标进入统计页，底部导航栏（首页/设置两个 Tab）仍然显示，与统计页自带的底部工具栏（粒度切换 + 时间翻页 + 视图切换）重叠。

**根因分析**：
统计页有自带的 `bottomBar`（`BottomToolbar`，含粒度选择、时间前后翻页、构成/趋势切换），无法与全局底部导航栏共存。修改时涉及两个独立操作：
1. 从 `bottomNavItems` 中移除统计 Tab（✅ 正确）
2. 统计页仍需保留在 `hideBottomBarRoutes` 中，隐藏全局底部栏（❌ 被错误地一并删除）

Agent 将两个操作错误地耦合——认为"既然 Tab 移除了，隐藏列表里也不需要了"。但这两个集合的语义完全不同：`bottomNavItems` 决定显示哪些 Tab，`hideBottomBarRoutes` 决定哪些页面不显示底部导航栏。一个页面可以既不在 Tab 列表中，又需要隐藏底部栏。

**解决方案**：
1. `bottomNavItems`：移除 `Route.Statistics`（底部栏只保留首页+设置）
2. `hideBottomBarRoutes`：**保留** `Route.Statistics`（统计页进入时隐藏全局底部栏）
3. `HomeScreen.kt`：TopAppBar 的 `actions` 中新增统计图标按钮（`BarChart`），作为进入统计页的主入口

**涉及文件**：
- `ui/navigation/MyPurseNavHost.kt`：`bottomNavItems` 移除统计 Tab，`hideBottomBarRoutes` 保留 `Route.Statistics`
- `ui/home/HomeScreen.kt`：TopAppBar 新增统计图标入口

**教训**：
- **不同语义的集合必须独立决策**：`bottomNavItems`（哪些 Tab）和 `hideBottomBarRoutes`（哪些页面隐藏栏）是两个独立维度，不能因为一个维度的修改就推断另一个维度也需要同步修改。每次修改只针对一个语义目标
- **修改前先理解数据结构之间的关系**：`bottomNavItems` 和 `hideBottomBarRoutes` 没有互斥关系——一个页面可以同时出现在两个集合中（如之前的统计页），也可以只出现在其中一个，还可以都不出现

---

### V1.0-#20：App 图标不显示 + 启动页显示异常（历经多次迭代修正）

- **发现日期**：2026-06-20
- **严重程度**：🟡 体验问题
- **所属模块**：应用资源 (res)、MainActivity

**现象**：
1. 桌面图标被系统加了绿色外圈和白色内圈，与用户提供的 `logopng.png`（圆形设计，米黄背景+绿色圆环+账本图案）不一致
2. 点击图标后，先闪现"浅绿背景+账本图标"，然后才切换到加载界面图（渐变+计算器）
3. 用户反馈加载界面图的渐变颜色看起来和原图不完全一致

**最终方案（经多轮迭代）**：

**图标修复**：
- 从 `logopng.png` 提取前景层（去除米黄色背景，保留绿色圆环+白色圆形+账本图案，其余透明）
- 在 `mipmap-anydpi-v26/` 提供符合规范的自适应图标配置（`ic_launcher.xml` + `ic_launcher_round.xml`）
- 背景层设为纯色 `#FCF4E2`（与 logopng 四角米黄色一致），前景层引用 `@mipmap/ic_launcher_foreground`
- 各 mipmap 密度目录提供 `ic_launcher_foreground.webp`（108/162/216/324/432dp）
- 颜色定义在 `values/colors.xml` 中：`ic_launcher_background = #FCF4E2`

**启动页修复**：
- 移除 `androidx.core.splashscreen` 库依赖（其 `windowSplashScreenBackground` 不支持 bitmap，导致显示应用图标而非加载界面图）
- 改用 Compose 层方案：`AppEntry()` composable 中直接用 `Image` + `ContentScale.FillBounds` 渲染 `splash_full.png`（从 `assets/加载界面.png` 复制），800ms 后切换到主界面
- Activity 的 Manifest theme 设为 `Theme.MyPurse.Splash`（`windowBackground = #E3F4EB`），`onCreate` 开头调用 `setTheme(Theme_MyPurse)` 切回主主题
- `#E3F4EB` 取自加载界面图顶部的近似纯色，减少背景色跳跃

**冷启动双画面闪现（已知限制）**：
- 点击图标后先出现"浅绿背景+账本图标"是 Android 系统的 Cold Start Preview Window，系统在进程启动前用 Manifest 中的 `android:icon` + Activity theme 的 `windowBackground` 渲染
- 这个行为是 Android 系统级的，代码层面无法完全消除。所有 Android 应用（包括微信、支付宝）在冷启动时都有类似现象
- 当前已做到的最优状态：背景色统一为 `#E3F4EB`，视觉过渡不突兀

**涉及文件**：
- `app/build.gradle.kts`：移除 splashscreen 依赖
- `app/src/main/AndroidManifest.xml`：Activity theme 改为 `Theme.MyPurse.Splash`
- `app/src/main/res/values/themes.xml`：Splash 主题 `windowBackground = #E3F4EB`
- `app/src/main/res/values/colors.xml`：新增 `ic_launcher_background = #FCF4E2`
- `app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`、`ic_launcher_round.xml`：自适应图标配置
- `app/src/main/res/mipmap-{density}/ic_launcher_foreground.webp`：各密度前景层
- `app/src/main/res/mipmap-{density}/ic_launcher.webp`、`ic_launcher_round.webp`：回退用图标
- `app/src/main/res/drawable-nodpi/splash_full.png`：加载界面图
- `app/src/main/java/com/wyd/mypurse/MainActivity.kt`：`AppEntry()` composable + `setTheme()` 主题切换

**教训**：
- Android 资源查找优先级：`mipmap-anydpi-v26` > `mipmap-xxxhdpi` > ... > `mipmap-mdpi`。替换图标时必须检查所有密度目录
- SplashScreen API 的 `animatedIcon` 是为小 logo 设计的，不适合整屏背景图
- 自适应图标（Adaptive Icons）需要提供 `mipmap-anydpi-v26/ic_launcher.xml`，不提供时 Pixel 等原生系统会强制套用圆形遮罩
- Pixel 模拟器（API 35）和国产手机（华为/小米/OPPO）的自适应图标行为一致，都遵循 Android 官方规范
- 冷启动 preview window 的图标闪现是系统行为，无法在代码层消除，只能通过统一背景色优化过渡
- 优先使用用户原始素材，避免 AI 二次加工衍生文件

---

### V1.0-#21：Release 签名构建失败 — PowerShell 转义导致 keystore 密码损坏

- **发现日期**：2026-06-20
- **严重程度**：🔴 阻塞（发布）
- **所属模块**：构建配置

**现象**：
执行 `.\gradlew assembleRelease` 报错 `keystore password was incorrect`，无法完成 Release 构建。

**根因分析**：
1. 用 PowerShell 执行 `keytool -storepasswd` 修改 keystore 密码时，新密码中含 `$` 符号
2. PowerShell 将 `$` 解释为变量前缀并展开，导致 keytool 实际收到的密码与预期不一致
3. jks 文件密码被改为一个"不可知"的值，之后任何密码都无法解锁
4. PowerShell 的 CLIXML 包装输出吞掉了 keytool 的真实错误信息（exitCode 始终为 0），进一步掩盖了问题

**解决方案**：
1. 删除已损坏的 `mypurse-release.jks`
2. 用 Python 的 `subprocess.run()` 直接调用 keytool（绕过 PowerShell 转义）重新生成 jks
3. 新密码使用纯字母数字（`eb78n5OOYd5xmbkWaLKz0dQ6`），不含特殊字符，彻底避免转义问题
4. 同步更新 `local.properties` 中的 `KEYSTORE_PASSWORD` 和 `KEY_PASSWORD`

**教训**：
- 在 PowerShell 中调用 keytool 等外部工具时，密码中避免使用 `$`、`^`、`|`、`&` 等特殊字符，或使用 Python subprocess 做中间层
- PowerShell CLIXML 输出会掩盖子进程的真实 exitCode 和错误信息——怀疑 keytool 结果时，用 Python subprocess 验证
- Gradle 的 `assembleRelease` 只在 `packageRelease` 阶段才校验签名密码，之前的 compile/ksp 阶段不会报错，导致问题发现滞后

---

### V1.0-#22：ColorConfig 顶层函数暴露

- **发现日期**：2026-06-20
- **严重程度**：🟡 体验问题（⚠️ 代码已回退）
- **所属模块**：UI 组件
- **说明**：ColorConfig.kt 中颜色获取函数应放在 object 内部，不应作为顶层函数暴露。Trae 会话 #26 修复后代码已回退。
- **状态**：低优先级，V1.1 再做

---

### V1.0-#23：版本更新流程过度设计

- **发现日期**：2026-06-20
- **严重程度**：🟢 优化建议
- **所属模块**：规范
- **说明**：版本更新流程过于复杂，不匹配当前单人开发规模。应精简为当前阶段适用流程。
- **状态**：待讨论

---

### V1.0-#24：开发日志会话顺序颠倒

- **发现日期**：2026-06-20
- **严重程度**：🟢 优化建议
- **所属模块**：文档
- **说明**：开发日志中会话条目时间顺序错误。已修复。
- **状态**：✅ 已解决

---

### V1.0-#25：Trae 代码改动因误操作全部回退

- **发现日期**：2026-06-20
- **严重程度**：🔴 阻塞（开发流程层面）
- **所属模块**：开发流程
- **说明**：会话 #24~#26 所有代码改动因 git 误操作全部回退。规范中增加了 13.2.1/13.2.2 防止再次发生。
- **状态**：✅ 已解决（规范层面）；部分有价值的修复在会话 #28 重新实施

---

### V1.0-#26：`CategoryDefDao` SQL NULL 比较语法错误 — 回退后重新修复

- **发现日期**：2026-06-20（Trae 会话 #24 发现），2026-06-21（CodeBuddy 重新修复）
- **严重程度**：🔴 阻塞
- **所属模块**：数据层 (CategoryDefDao)

**现象**：
`getCategoryByName` 和 `getCategoryByNameAndSign` 使用 `parent_id IS :parentId`。当 `parentId` 为 `null` 时行为正常（等价于 `IS NULL`），但当 `parentId` 为非空值（如 `123L`）时，SQL 变成 `parent_id IS 123`——这是非法 SQL 语法。

**根因分析**：
SQL 中 `IS` 关键字只能与 `NULL`、`TRUE`、`FALSE` 搭配使用，不能与绑定参数混用。Room 编译阶段不会检查 SQL 语义错误，运行时才会暴露。

**修复**：
将 `parent_id IS :parentId` 改为 `(parent_id = :parentId OR (parent_id IS NULL AND :parentId IS NULL))`。这个写法正确处理了两种情况：当 `parentId` 非 null 时用 `=` 比较，当 `parentId` 为 null 时用 `IS NULL` 匹配。

**涉及文件**：
- `data/local/dao/CategoryDefDao.kt`：第 50、54 行 SQL 修正

**教训**：
- Room 的 `@Query` 注解中的 SQL 是字符串，编译期只做基本语法检查，不做语义验证
- `IS` 和 `=` 在 SQL 中是不同的运算符：`IS` 用于 NULL 比较，`=` 用于值比较
- 写涉及 nullable 参数的 Room 查询时，必须显式处理 NULL 和非 NULL 两种情况

---

### V1.0-#27：`BudgetViewModel` 架构违规 — 直接注入 DAO

- **发现日期**：2026-06-20（Trae 会话 #24 发现），2026-06-21（CodeBuddy 重新修复）
- **严重程度**：🟠 功能缺陷（架构层面）
- **所属模块**：预算功能 (BudgetViewModel)

**现象**：
`BudgetViewModel` 直接注入 `BudgetDao`，绕过了 Repository 抽象层。而 `TransactionRepository` 接口中已有 `getBudget()` 和 `observeBudget()` 方法，但未被使用。

**根因分析**：
预算功能开发时（阶段 5），可能是为了快速实现，直接在 ViewModel 中注入了 DAO。后来 `TransactionRepository` 接口中补充了预算相关方法，但 ViewModel 没有同步更新依赖。

**修复**：
1. `TransactionRepository` 接口新增 `updateBudget(amount)` 和 `deleteBudget()` 方法
2. `TransactionRepositoryImpl` 实现这两个方法
3. `BudgetViewModel` 依赖从 `BudgetDao` 改为 `TransactionRepository`，所有 DAO 调用改为 Repository 方法调用

**涉及文件**：
- `domain/repository/TransactionRepository.kt`：新增两个预算方法声明
- `data/repository/TransactionRepositoryImpl.kt`：实现新增方法
- `ui/budget/BudgetViewModel.kt`：依赖替换

**教训**：
- 架构违规往往不是故意为之，而是"快速实现"的遗留产物。每次新增 Repository 方法后，应检查所有 ViewModel 是否已使用
- 规范 2.1 节明确要求依赖方向 `ui → domain ← data`，ViewModel 不应知道 DAO 的存在

---


### V1.0-#28：全局颜色硬编码 — TransactionDetailScreen 5 处

- **发现日期**：2026-06-21
- **严重程度**：🟡 体验问题（功能正常但维护性差）
- **所属模块**：流水详情页 (D)

**现象**：
`TransactionDetailScreen` 中 5 处直接写了 `Color(0xFFxxxxxx)` 色值，而非引用 `AppColors.kt` 中的统一颜色变量。

**根因分析**：
V1.0 开发初期没有建立"颜色从 AppColors 统一取"的规范，各页面各自写色值。`AppColors.kt` 中已有对应颜色变量（如 `AppBudgetBlue`、`AppExpenseRed`），但详情页没有使用。

**修复**：
5 处硬编码色值替换为 `AppColors.kt` 中的对应变量。

**涉及文件**：
- `ui/transaction/TransactionDetailScreen.kt`：5 处色值替换

**教训**：
- 新增页面时，应先检查 `AppColors.kt` 是否已有可用变量，而非直接写色值
- 举一反三：同理，`dimens.xml`（如有）、`strings.xml` 也应作为"先查再用"的清单

---

### V1.0-#29：分类颜色体系缺失 — 5 个页面各自为政

- **发现日期**：2026-06-21
- **严重程度**：🟠 功能缺陷（设计层面，非 crash）
- **所属模块**：全局（分类展示、统计图表、首页排行）

**现象**：
`Category` 数据模型没有 `color` 字段。5 个页面对分类颜色的处理各自独立：

| 页面 | 颜色来源 | 特点 |
|---|---|---|
| 首页 TOP5 排行 | `AppBudgetBlue` 固定蓝色 | 所有分类同色，无区分 |
| 统计页构成分析 | `chartPalette[index]` 按排名轮询 | 颜色与分类名无关（第1名蓝、第2名绿…） |
| 记一笔分类选择 | `surfaceVariant` 统一灰色 | 无颜色区分 |
| 分类管理页 | `surfaceVariant` 统一灰色 | 无颜色区分 |
| 流水列表 | 支出红/收入绿 二元 | 仅区分收支方向 |

**根因分析**：
项目从一开始就没有"分类应有固定颜色"的设计意识。V1.0 阶段专注功能实现，颜色是可访问性层面的"锦上添花"，被搁置了。等到想做"餐饮=橙、交通=蓝"的统一配色时，发现需要改 5 个页面。

**影响**：
1. 用户无法通过颜色快速识别分类（排行、统计图、分类选择器）
2. 想做"分类颜色自定义"时，需要先在 5 个页面逐一改造
3. 统计页 `chartPalette[index]` 按排名分配颜色的逻辑，与分类语义无关，换一个月颜色就会乱

**修复**（2026-06-21 完成）：
1. Category 模型新增 `color: Long = 0` 字段
2. CategoryDefEntity 新增 `color` 列（MIGRATION_4_5，数据库版本升至 5）
3. CategoryDefaults 种子数据为 17 个内置分类定义了固定颜色（支出侧暖色系、收入侧冷色系）
4. CategoryAmount / CategoryAmountEntity 新增 `color` 字段，SQL 查询 JOIN category_def 取色
5. 统计页构成分析：chartPalette[index] → categoryColor(item.color)（fallback 保留 chartPalette）
6. 首页 TOP5 排行：AppBudgetBlue → categoryColor(category.color)（fallback 保留 AppBudgetBlue）
7. 记一笔分类选择、分类管理页、流水列表暂不改动（改动范围小，颜色体系已建立，后续按需改造）
8. 为 V1.1 用户自定义颜色预留：Category.color 字段 + categoryColor() 工具函数，DataStore 覆盖层后续加

**涉及文件（10 个）**：
- `domain/model/Category.kt`：新增 `color: Long = 0`
- `domain/model/Transaction.kt`：`CategoryAmount` 新增 `color: Long = 0`
- `data/local/entity/CategoryDefEntity.kt`：新增 color 列
- `data/local/entity/CategoryAmountEntity.kt`：新增 `val color: Long = 0`
- `data/local/database/AppDatabase.kt`：version 升至 5，新增 MIGRATION_4_5
- `data/local/dao/TransactionDao.kt`：3 个聚合查询 JOIN category_def 取 color
- `data/repository/CategoryDefaults.kt`：种子数据加颜色
- `data/repository/CategoryRepositoryImpl.kt`：toDomain/toEntity 映射 color
- `data/local/database/DatabaseInitializer.kt`：首次播种时写入 color
- `ui/theme/AppColors.kt`：新增 `categoryColor()` 工具函数
- `ui/statistics/StatisticsScreen.kt`：3 处颜色改为 `categoryColor(item.color)`
- `ui/home/HomeScreen.kt`：CategoryRankRow 新增 `color` 参数

**教训**：
- 涉及"展示"的数据属性（颜色、图标、排序），应在数据模型中预留字段，即使 V1.0 暂不展示
- 任何涉及多个页面的视觉元素，必须从统一数据源取，禁止各自实现（已沉淀到规范 2.3.5、2.3.6）
- 扩展性考量：color 使用 Long 类型（非 Compose Color）保持 domain 层零 Android 依赖

---

### V1.0-#30：categoryColor() 使用 Color(argb.toULong()) 导致闪退

- **发现日期**：2026-06-21
- **严重程度**：🔴 阻塞
- **所属模块**：全局（AppColors.kt → 所有使用 categoryColor 的页面）

**现象**：
App 启动后立即闪退，崩溃日志：
```
FATAL EXCEPTION: main
java.lang.ArrayIndexOutOfBoundsException: length=18; index=58
  at Color.getColorSpace-impl(Color.kt:724)
  at Color.convert-v2Lc2BU(Color.kt:1064)
  at Color.toArgb-0d7_KjU(Color.kt:304)
  at AndroidPaint.setColor-8_81llA(AndroidPaint.android.kt:86)
  at Canvas.drawLine(Canvas.kt:382)
  at LinearProgressIndicator.drawLinearIndicator(ProgressIndicator.kt:458)
```
崩溃链全部在 Compose 框架内部，无任何 com.wyd.mypurse 帧。

**根因分析**：

AppColors.kt 中：
```kotlin
fun categoryColor(argb: Long): Color? = if (argb == 0L) null else Color(argb.toULong())
```

Compose Color 有多个构造器重载：
- Color(value: Long) — 期望 32 位 SRGB ARGB 整数（高位 32 位填 0），这是正确的用法
- Color(red: Float, green: Float, blue: Float, alpha: Float, colorSpace: ColorSpace) — 浮点分量
- **没有 Color(ULong) 构造器**

传入 ULong 时，Kotlin 编译器将其转为 Long，但位表示可能发生变化。导致 Color 内部表示中的 colorSpace 索引字段被写入了非法值（如 58），而 colorSpace 查找表只有 18 个条目，渲染时数组越界崩溃。

**修复**：
将 Color(argb.toULong()) 改为 Color(argb)（直接传 Long），编译安装后不再崩溃。
```kotlin
fun categoryColor(argb: Long): Color? = if (argb == 0L) null else Color(argb)
```

**涉及文件**：
- `ui/theme/AppColors.kt`：argb.toULong() → argb

**教训**：
1. **不要使用不存在的构造器重载**：Compose Color 没有 Color(ULong) 构造器。应先查阅 API 文档确认有哪些重载
2. **ARGB 颜色值用 Long 而非 ULong**：Color(Long) 是标准构造器，ULong 不是 Compose Color API 的一部分
3. **跨类型转换时警惕位布局变化**：Kotlin 的 Long → ULong → Long 往返不保证恒等
4. **"编译通过 ≠ 运行正确"**：Kotlin 编译器对 inline value class 的类型擦除可能掩盖问题
5. **崩溃全在框架内部帧时应首先怀疑参数类型**：当崩溃堆栈完全没有自己代码的帧时，问题往往出在传入框架 API 的参数类型/格式不符合框架预期

**举一反三**：
- 项目中所有其他 Color(0xFFxxxxxx) 调用使用的是 Int 字面量，不受影响
- 后续涉及 Long → Color 转换时，应始终使用 Color(Long) 而非 Color(ULong)
- 类似风险：Dp(Float) vs Dp(Double)，TextUnit(Int) vs TextUnit(Float) 等 Compose inline class 的类型转换

---

### V1.0-#31：时间选择器重复实现 — 3 套独立组件

- **发现日期**：2026-06-21
- **严重程度**：🟡 体验问题（功能正常但存在维护风险）
- **所属模块**：全局（流水列表、统计页）

**现象**：
项目中有 3 套独立的时间选择器实现：
1. ChineseDatePickerDialog — 记一笔、流水列表用（公共组件）
2. WheelPickerForStat — 统计页用（#17 已修复合并）
3. 年份选择器 — 统计页 3 处 + 流水列表 1 处各自实现

**根因分析**：
各页面开发时未考虑"是否已有相同功能的组件"，直接复制粘贴修改。这是"同功能同组件"原则缺失的典型表现。

**修复**：
- #17 已将 WheelPickerForStat 合并到公共 WheelPicker
- #35（2026-06-21）已完成全面统一：`YearMonthWheelSheet` 和 `YearWheelSheet` 提取为公共组件（ChineseDatePickerDialog.kt），所有页面（流水列表、统计页、记一笔）复用。删除 4 个私有重复组件：YearSheet、YearMonthSheet、YearWheelSheet（StatisticsScreen.kt）、YearMonthPickerDialog、YearPickerDialog（TransactionListScreen.kt）。

**涉及文件**：
- `ChineseDatePickerDialog.kt`：YearMonthWheelSheet 从 private→internal，新增 title/highlightColor/selectedTextColor 等参数化；新增 YearWheelSheet 公共组件
- `StatisticsScreen.kt`：删除 YearSheet、YearMonthSheet、YearWheelSheet，改为 import 公共组件
- `TransactionListScreen.kt`：删除 YearMonthPickerDialog、YearPickerDialog，改为 import 公共组件

**教训**：
- 开发新功能前，应先搜索项目中是否已有相同/相似功能的组件
- "复制一份改改"是技术债务的最快积累方式

---
## 项目回顾：V1.0 开发经验总结

> 以下内容在 V1.0 发布后（2026-06-20）通过系统性回顾总结得出。总结方法：对照需求文档、开发阶段、问题记录、验收清单，回答"如果重来一次，哪些事一开始就可以做好"。

### 一、需求层面 — "减法前置"

**现象**：V1.0 开发过程中出现了大量"做减法"的决策——砍对比分析、简化预算模型从按月到单一值、简化分类分组从树形到三选项、推迟固定收支模板、移除统计页预算剩余——这些决策都发生在开发进行到一半以后。

**经验**：**在需求阶段就做减法，而非开发到一半发现复杂度失控再做。** 具体方法：每提一个功能，追问"如果不做这个，用户会痛吗？"——会痛的保留，不会痛的推迟。

**一开始就可以砍掉的**：
| 功能 | 为什么 |
|------|--------|
| 对比分析 | "花在哪"+"怎么变"已覆盖核心诉求，对比分析信息增量有限 |
| 按月独立预算 | 个人记账几乎不回看历史预算，单一值的复杂度收益不成比例 |
| 分类树形分组 | 三选项（时间/一级/二级）已覆盖全部核心场景 |
| 固定收支模板 | 场景占比仅 10%，但时间周期计算复杂度极高 |
| 统计页预算剩余 | 预算属前瞻性指标，放统计页语义不匹配 |

### 二、架构层面 — "复用优先"意识

**现象**：问题 #16（编辑流水记录页代码重复）、#17（WheelPickerForStat 是公共组件复制品）暴露了同一个模式——遇到类似需求时，第一反应是"复制一份改改"。

**经验**：**在写第一个"看起来像能复用的组件"时，就把它放到公共目录并写成参数化的。** 事后迁移的成本远高于一开始就做对。具体规范已沉淀到开发规范 2.3「公共组件复用原则」。
- `WheelPicker` 应从一开始就设计为通用组件，通过参数控制行高、颜色、显示模式
- "新建/编辑"页面应从一开始就是同一个 Composable，通过 `editingRecord: Transaction?` 区分模式
- `DatePicker`、`NumberKeyboard` 应从一开始就在 `ui/components/` 下

### 三、数据库层面 — 避免破坏性迁移

**现象**：问题 #6 中，开发初期用了 `fallbackToDestructiveMigration()`，后来加表时数据全部丢失。

**经验**：`fallbackToDestructiveMigration()` 只应在 version=1 时使用一次。一旦 version 升到 2，就必须写 Migration。这个习惯从项目第 1 天就该建立。开发阶段也应接入 CSV 导出备份，即使数据不重要，也要养成习惯。

### 四、Git 使用层面 — "高频提交"纪律

**现象**：问题 #18 是项目影响面最大的单次事故——一次 `git checkout --` 导致跨会话的数天工作成果丢失。根因是 AI Agent 阶段式开发模式下，多个独立会话共享同一工作区，长时间不提交导致累积成果暴露在风险中。

**经验**：**在 AI Agent 协作模式下，Git 不是"做完再提交"的工具，而是"随时保护累积成果"的保险。**
- 每个阶段完成后立即 commit（已写入规范 6.0）
- 提交粒度与会话边界对齐——每次 Agent 会话结束时至少 commit 一次
- 回退操作前做快照（`git stash` 或 `git diff > backup.patch`）
- 颜色/主题等全局改动在独立 commit 中完成

### 五、验收清单 — "随代码同步维护"

**现象**：V1.0 发布前发现验收清单中有多项与代码实际状态不一致——饼图未实现但清单中有、查看分类流水跳转不存在但清单中有、批量修改分类不存在但清单中有。这些项是 Agent 根据需求文档生成的，从未与代码做过交叉验证。

**经验**：**验收清单的生命周期应与开发阶段同步，而非滞后于开发。** 具体做法：阶段 N 完成后 → 更新阶段 N 对应的验收项状态 → 删除/修正不准确项 → 再进入阶段 N+1。

### 六、可推广到其他项目的通用经验

| # | 经验 | 适用场景 |
|---|------|---------|
| 1 | **需求阶段就做减法**：每提一个功能追问"不做会痛吗"，用 ROI 视角审视 | 任何 MVP 项目 |
| 2 | **第一个可复用组件就放公共目录**：不要等"以后再说"，以后迁移成本高 | 任何多页面项目 |
| 3 | **数据库 Migration 从 version=2 就写**：fallbackToDestructive 只用于 version=1 | 任何使用 ORM 的项目 |
| 4 | **Agent 会话结束时必须 commit**：跨会话的累积成果需要 git 保护 | AI Agent 协作模式特有 |
| 5 | **验收清单随阶段同步维护**：不应滞后于开发，否则会失真 | 任何有验收清单的项目 |
| 6 | **变更决策即时记录**：不要攒到阶段结束再补，会遗漏细节和决策上下文 | 任何有需求变更的项目 |
| 7 | **Compose 状态管理陷阱**：`rememberUpdatedState`、`derivedStateOf`、`snapshotFlow` 的边界行为需要提前理解 | Android Compose 项目 |
| 8 | **PowerShell 转义坑**：密码中避免 `$` `^` `|` `&` 等特殊字符，或用 Python 做中间层 | Windows + Gradle 构建环境 |

### 总结一句话

**如果重来一次，最大的改进不是技术选择，而是"减法前置"和"纪律前置"——在需求阶段就砍掉 ROI 低的功能，在项目第 1 天就建立 git commit、组件复用、Migration 写法的纪律。** 这些事不需要更多技术能力，只需要认知上的"提前一步"。

---

> 最后更新：2026-06-21
