# MyPurse 开发阶段规划

> **文档版本**：V1.4 (2026-06-21)

---

## 阶段总览

| 阶段 | 内容 | 状态 |
|---|---|---|
| 阶段 0 | 项目初始化（Gradle、Hilt、Room、Navigation） | ✅ 完成 |
| 阶段 1 | 数据层 + 领域层基础（Entity、Dao、Repository、Model） | ✅ 完成 |
| 阶段 2 | 首页 (A) + 记一笔 (B) | ✅ 完成（含 2.1：支出/收入分类分离） |
| 阶段 3 | 流水列表 (D) + 详情页 | ✅ 完成（2026-06-16） |
| 阶段 4 | 统计页 (C) — 构成分析 + 趋势分析 | ✅ 完成（2026-06-16） |
| 阶段 5 | 预算设置页 (F) | ✅ 完成（2026-06-16） |
| 阶段 6 | 分类管理页 (E) | ✅ 完成（2026-06-17） |
| 阶段 7 | 固定收支模板 (G) + 自动记账 | ✅ 完成（V1.1 阶段 11） |
| 阶段 8 | 设置页 + CSV 导入/导出 + 数据管理 | ✅ 完成（2026-06-17） |
| 阶段 9 | 收尾优化（空状态、错误处理、性能、边界、代码清理、验收） | ✅ 完成（2026-06-17） |
| 阶段 10 | V1.0.1 体验优化（分类管理交互、统计页时间选择器统一、UI风格、数据可视化） | ✅ 完成 |
| 阶段 11 | V1.1 多套主题风格 + 固定收支自动记账 | ✅ 完成 |

---

## 阶段 0：项目初始化

**状态**：✅ 完成

| 事项 | 说明 |
|---|---|
| Gradle 构建脚本 | `build.gradle.kts`、`settings.gradle.kts`、`gradle.properties` |
| 依赖管理 | `gradle/libs.versions.toml`（版本目录） |
| Hilt 依赖注入 | `MyPurseApp.kt`、`di/` 模块 |
| Room 数据库 | `AppDatabase.kt`、实体、DAO、TypeConverter |
| Jetpack Navigation | `MyPurseNavHost.kt`、`Route.kt`、占位页面壳子 |
| Material3 主题 | `Theme.kt`、`Color.kt`、`Type.kt` |

---

## 阶段 1：数据层 + 领域层基础

**状态**：✅ 完成

### 数据实体（Entity）

| 文件 | 对应表 |
|---|---|
| `TransactionEntity.kt` | `transaction` — 收支记录 |
| `CategoryDefEntity.kt` | `category_def` — 分类定义（两级） |
| `CategoryAmountEntity.kt` | 预算查询中间结果（非表） |
| `BudgetEntity.kt` | `budget` — 月度预算 |

### DAO

| 文件 | 说明 |
|---|---|
| `TransactionDao.kt` | 增删改查 + 聚合查询（余额、汇总、趋势、排行） |
| `CategoryDefDao.kt` | 分类 CRUD + 一次性查询 |
| `BudgetDao.kt` | 预算读写 + 按月查询 |

### 领域模型（Domain Model）

| 文件 | 说明 |
|---|---|
| `Transaction.kt` | 交易记录领域模型 |
| `Category.kt` | 分类领域模型 |
| `FlowType.kt` | 流水类型（支出/收入） |

### 仓库接口与实现

| 文件 | 说明 |
|---|---|
| `TransactionRepository.kt` | 交易仓库接口 |
| `TransactionRepositoryImpl.kt` | 交易仓库实现 |
| `CategoryRepository.kt` | 分类仓库接口 |
| `CategoryRepositoryImpl.kt` | 分类仓库实现 |
| `FlowTypeRepository.kt` | 流水类型仓库接口 |
| `FlowTypeRepositoryImpl.kt` | 流水类型仓库实现 |

### 用例（UseCase）

| 文件 | 说明 |
|---|---|
| `GetHomeDataUseCase.kt` | 聚合首页所需全部数据 |

### 依赖注入

| 文件 | 说明 |
|---|---|
| `DatabaseModule.kt` | Room 数据库 + DAO 提供 |
| `RepositoryModule.kt` | 仓库绑定 |

---

## 阶段 2：首页 (A) + 记一笔 (B)

**状态**：✅ 完成

### 首页 (A) — `ui/home/`

| 文件 | 说明 |
|---|---|
| `HomeScreen.kt` | 首页 UI：概览卡片、日/周/月/年汇总、预算进度条、6月趋势柱状图、TOP5 排行、FAB |
| `HomeViewModel.kt` | 首页 ViewModel：加载数据、状态管理 |
| `HomeUiState.kt` | 首页状态类 |

**已实现的 UI 行为**：

| 元素 | 行为 |
|---|---|
| 概览卡片 | 显示当前结余（正/负颜色），可点击跳转流水列表 |
| 今日/本周/本月/今年汇总 | 四行显示支出（红）和收入（绿），可点击跳转 |
| 预算进度条 | 未设置 → 引导文字；<80% 蓝色；80-100% 橙色；≥100% 红色 |
| 近6月趋势图 | 柱状图 + 月份标签，空数据隐藏 |
| TOP5 排行 | 分类名 + 金额 + 占比% + 比例条，空数据隐藏 |
| FAB (+) | 跳转记一笔页 |

### 记一笔 (B) — `ui/addtransaction/`

| 文件 | 说明 |
|---|---|
| `AddTransactionScreen.kt` | 记一笔 UI：自定义数字键盘、流水类型（支出/收入两个按钮）、两级分类选择器（按流水类型自动过滤）、日期、备注、保存 |
| `AddTransactionViewModel.kt` | 记一笔 ViewModel：连续记账、保存并关闭、根据 FlowType.sign 加载对应分类 |
| `AddTransactionUiState.kt` | 记一笔状态管理 |

**已实现功能**：

| 功能 | 说明 |
|---|---|
| 自定义数字键盘 | 支持小数点后两位，删除键 |
| 流水类型切换 | 支出/收入两个按钮，选中高亮，切换时自动切换对应分类列表并清空已选分类 |
| 两级分类选择器 | Bottom Sheet 弹出，一级网格 → 二级网格，含"跳过细项"。分类按 flowSign 过滤，支出和收入独立 |
| 日期选择器 | Material3 DatePickerDialog，默认当天 |
| 备注输入 | 选填文本 |
| 连续记账 | 保存后清空金额和备注，保留类型和分类 |
| 保存并关闭 | 保存后返回首页 |

**阶段 2.1 新增：支出/收入分类分离**（2026-06-15）

| 改动 | 说明 |
|---|---|
| `CategoryDefEntity.flowSign` | 新增字段：-1=支出侧，1=收入侧，0=通用（预留） |
| `Category.flowSign` | 领域模型同步新增 |
| `CategoryDefaults` | 拆分支出侧种子数据（9个一级+二级）和收入侧种子数据（8个一级+二级） |
| `CategoryDefDao` | 新增 `getTopLevelCategoriesBySign` / `getTopLevelCategoriesBySignOnce` |
| `CategoryRepository` | 新增 `getTopLevelCategoriesBySign` / `getTopLevelCategoriesBySignOnce` |
| `DatabaseInitializer` | 同时插入支出侧和收入侧种子数据，flowSign 正确赋值 |
| `AppDatabase` | 版本升级到 2 |
| `AddTransactionViewModel` | 移除 `FlowTypeRepository` 依赖，切换流水类型时根据 sign 加载对应分类 |
| `AddTransactionUiState` | 移除 `flowTypes` 列表（支出/收入硬编码） |
| `AddTransactionScreen` | 流水类型改为两个等宽按钮（支出/收入），不再动态列表 |
| `FlowTypeRepository` | 接口保留（预留扩展），注释标注 V1.0 不开放自定义 |
| `FlowTypeRepositoryImpl` | 简化为仅返回内置支出/收入，移除 SharedPreferences 逻辑 |

### 阶段 2 新增/修改的数据层

| 文件 | 变更 |
|---|---|
| `CategoryDefDao.kt` | 新增 `getSubCategoriesOnce()` |
| `CategoryRepository.kt` | 新增 `getSubCategoriesOnce()` 接口 |
| `CategoryRepositoryImpl.kt` | 实现 `getSubCategoriesOnce()` |

---

## 阶段 3：流水列表 (D) + 详情页

**状态**：✅ 完成（2026-06-16）

### 流水列表页 (D) — `ui/transactionlist/`

| 功能 | 说明 |
|---|---|
| 列表展示 | 分类图标、分类名、金额（红负绿正）、备注、日期、🔄标记 |
| 默认分组 | 按月分组，组内按日期倒序，同天按创建时间倒序 |
| 时间粒度切换 | 日/周/月/年 四个 Tab，默认月 |
| 年视图特殊处理 | 每月合计摘要，点击钻取到月视图 |
| 分类筛选 | 全部分类 / 按一级分类筛选，与时间粒度可组合 |
| 搜索 | 实时过滤，匹配备注、分类名、金额 |
| 空状态 | 无记录 / 无筛选结果 / 无搜索结果 三种提示 |
| 入口默认状态 | 从首页进入自动设置对应视图和筛选 |
| 条目交互 | 点击进入详情；左滑删除/修改 |
| 删除确认 | 手动记录/自动记录 不同弹窗提示 |

### 详情页

| 功能 | 说明 |
|---|---|
| 只读展示 | 全部字段展示 |
| 编辑模式 | 右上角"编辑"，字段可编辑 |
| 自动记录提示 | 编辑自动记录时提示解除模板关联 |

### 性能要求

| 指标 | 目标 |
|---|---|
| 分页加载 | Room Flow + 手动分页，按月分段 |
| 预加载 | 相邻月份数据预加载 |
| 大数据量 | 50,000 条无卡顿 |

---

## 阶段 4：统计页 (C)

**状态**：✅ 完成（2026-06-16）

### 整体架构

两个子视图，左右滑动切换：
- **构成分析**：钱花在哪？— 横向条形图（主力）+ 饼图（辅助）
- **趋势分析**：支出随时间变化？— 柱状图 + 折线图叠加
- ~~对比分析~~：已从需求中移除（2026-06-16 需求变更）

### 时间维度选择器（三视图共享）

| 粒度 | 默认值 | 切换方式 |
|---|---|---|
| 日 | 今天 | 左右箭头逐天，点击弹日历 |
| 周 | 本周 | 左右箭头逐周，点击弹周选择器 |
| 月 | 本月 | 左右箭头逐月，点击弹年月选择器 |
| 季 | 本季 | 左右箭头逐季，点击弹季选择器 |
| 年 | 今年 | 左右箭头逐年，点击弹年份选择器 |

### 构成分析

| 区域 | 描述 |
|---|---|
| 支出/收入 Tab | 顶部切换 |
| 横向条形图 | 按金额从高到低，显示金额+占比% |
| 二级分类展开 | 点击一级展开二级明细，再次点击收起 |
| 饼图切换 | 右上角按钮切换 |
| 跳转入口 | 底部"查看该分类全部流水 >" |

### 趋势分析

| 区域 | 描述 |
|---|---|
| 图表模式 | 纯柱状 / 纯折线 / 叠加（默认），三按钮切换 |
| X 轴数据点 | 日=30天、周=12周、月=12月、季=8季、年=全部 |
| 悬浮提示 | 点击数据点弹出摘要（总支出、TOP3分类） |
| 数据摘要 | 均值、最高月份、最低月份、环比变化 |
| 预算线 | 不显示（统计页不做预算展示） |

### 技术选型

- 图表：Compose Canvas 原生绘制（未引入第三方图表库）
- 聚合计算：Room `@Query` SQL 层完成
- 用例封装：`GetStatisticsUseCase`

---

## 阶段 5：预算设置页 (F)

**状态**：✅ 完成（2026-06-16）

### 功能清单

| 功能 | 说明 |
|---|---|
| 金额输入 | 数字键盘，¥ 前缀，占位符 0.00 |
| 快捷设置 | 3 个预设金额按钮（1000/5000/10000），点击快速填入，每个按钮旁编辑图标可自定义 |
| 保存按钮 | 保存预算，成功后弹出确认弹窗。金额为 0 时按钮置灰 |
| 不设预算 | 已设预算时显示红色按钮，点击清除预算值 |

### 设计要点

- 预算采用极简单一值存储（budget 表 id=1 唯一记录）
- 修改即覆盖，无历史追踪
- 首页预算进度条：<80% 蓝色，80-100% 橙色，≥100% 红色
- 无预算时首页显示引导文字"设置月度预算 >"

---

## 阶段 6：分类管理页 (E)

**状态**：✅ 完成（2026-06-17）

### 功能清单

| 功能 | 说明 |
|---|---|
| 恢复默认分类 | 一键还原内置分类（跳过已存在） |
| 一级分类列表 | 图标、名称、二级数量，拖拽排序 |
| 二级分类列表 | 展开显示，拖拽排序 |
| 左滑操作 | 编辑/删除 |
| 新增分类 | 选择级别、输入名称、选择图标 |
| 删除分类 | 三个选项：①直接删除（保留记录）②迁移至其他 ③删除分类及记录 |
| 修改名称 | 仅影响后续，历史保留快照 |
| 批量修改分类 | 按原分类名批量替换，支持时间范围过滤，预览影响条目数 |
| 无分类引导 | 记一笔无分类时弹出引导并跳转 |

---

## 阶段 7：固定收支模板 (G) + 自动记账

**状态**：🔲 待开发

### 模板列表页

| 功能 | 说明 |
|---|---|
| 新增模板按钮 | + 新增 |
| 已启用列表 | 卡片：图标、名称、周期描述、金额、下次执行日期、开关 |
| 已暂停列表 | 折叠显示，灰显 |
| 左滑操作 | 编辑/删除/暂停-启用 |
| 删除确认 | 仅删除模板 / 删除模板及关联记录 |
| 空状态 | "暂无固定模板，点击 + 新增" |

### 新增/编辑模板页

| 字段 | 说明 |
|---|---|
| 流水类型 | 支出/收入/自定义 |
| 金额 | 数字键盘 |
| 分类 | 分类选择器 |
| 周期 | 每天/每周/每月/每年 |
| 执行日 | 根据周期动态显示 |
| 备注 | 选填 |
| 启用开关 | 开启/暂停 |
| 下次执行日期 | 自动计算，只读 |

### 自动记账引擎

| 功能 | 说明 |
|---|---|
| 触发时机 | 每次 App 启动时检查 |
| 补记策略 | 最多补记过去 90 天 |
| 防重复 | 通过 `recurringTemplateId` 检查 |
| 无效日期处理 | 如每月31日 → 当月最后一天 |
| 与流水联动 | 自动记录带 🔄 标记，编辑后解除关联 |

---

## 阶段 8：设置页 + 数据管理

**状态**：✅ 完成（2026-06-17）

### 功能清单

| 分组 | 项目 | 说明 |
|---|---|---|
| 数据管理 | 导出 CSV | 全量导出为 UTF-8 BOM CSV，系统分享 |
| | 导入 CSV | SAF 文件选择 → 校验（表头/类型/金额/日期）→ 逐条追加（自动创建不存在的分类）→ Toast 反馈结果 |
| | 清除所有数据 | 二次确认 → 清空 4 张表 → 重新播种内置分类 |
| 记账设置 | 分类管理 | 跳转 E ✅ |
| | 预算设置 | 跳转 F ✅ |
| | 固定收支模板 | 已移除（推迟到 V1.1） |
| 显示设置 | 暗黑模式 | V1.1（灰显"即将推出"） |
| | 主题风格 | V1.1（灰显"即将推出"） |
| | 统计图配色 | V1.1（灰显"即将推出"） |
| 关于 | 版本号 | v1.0.0 |
| | 开源许可 | 查看开源许可证（独立 AboutScreen） |
| | 反馈与建议 | 打开邮件客户端 |

### 新增/修改文件

| 文件 | 操作 |
|---|---|
| `ui/settings/SettingsViewModel.kt` | 新建 — 导出/导入/清除全部逻辑 |
| `ui/settings/SettingsUiState.kt` | 新建 — UI 状态 |
| `ui/settings/SettingsScreen.kt` | 重写 — 分组设置页完整 UI |
| `ui/about/AboutScreen.kt` | 重写 — 版本号、开源许可、说明 |
| `data/export/CsvExporter.kt` | 新建 — CSV 生成（UTF-8 BOM、双引号转义） |
| `data/export/CsvImporter.kt` | 新建 — CSV 解析 + 校验（表头匹配、类型/金额/日期校验） |
| `data/local/dao/TransactionDao.kt` | 新增 — getAllTransactionsOnce、getTransactionsByRangeOnce、deleteAllTransactions、getTransactionCount |
| `data/local/dao/CategoryDefDao.kt` | 新增 — deleteAllCategories |
| `data/local/dao/BudgetDao.kt` | 新增 — deleteAllBudgets |
| `data/local/dao/RecurringTemplateDao.kt` | 新增 — deleteAllTemplates |
| `data/local/database/DatabaseInitializer.kt` | 新增 — forceReinitialize（清空后重新播种） |
| `domain/repository/TransactionRepository.kt` | 新增 — getAllTransactionsOnce、getTransactionsByRangeOnce、clearAllData、getTransactionCount |
| `data/repository/TransactionRepositoryImpl.kt` | 实现以上方法，注入 CategoryDefDao/RecurringTemplateDao/DatabaseInitializer |
| `ui/navigation/MyPurseNavHost.kt` | 更新 Settings 回调注释 |
| `AndroidManifest.xml` | 新增 FileProvider（CSV 导出分享） |
| `res/xml/file_paths.xml` | 新建 — FileProvider 路径配置 |

---

## 阶段 9：收尾优化

**状态**：✅ 完成（2026-06-17）

按优先级分 4 个子阶段：

### 9a：空状态 + 错误处理 + Migration（用户直接感知，最高优先级） ✅ 完成

| # | 事项 | 说明 | 状态 |
|---|---|---|---|---|
| 9a-1 | 全页面空状态检查 | 初始空状态 + 操作后空状态，含文案和引导按钮 | ✅ 完成 |
| 9a-2 | 首次使用引导 | 新用户零数据状态引导（记第一笔、设置预算） | ✅ 完成 |
| 9a-3 | 数据库异常兜底 | 数据库损坏时捕获异常，自动删除损坏文件并重建 | ✅ 完成 |
| 9a-4 | CSV 导出失败反馈 | 文件写入失败时明确 Toast 提示 | ✅ 已有（try-catch + errorMessage Toast） |
| 9a-5 | 数据库 Migration 正式化 | 替换 `fallbackToDestructiveMigration()` 为正式 Migration | ✅ 完成 |

### 9b：性能验证 + 快速操作防护 ✅ 完成

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 9b-1 | 冷启动性能 | 目标 ≤1s，测试设备：模拟器 API 24 低配配置 | 🔲 需手动验证 |
| 9b-2 | 列表帧率 | 插入 5000 条记录后测试滚动帧率，目标 60fps | 🔲 需手动验证 |
| 9b-3 | 统计图加载 | 目标 ≤1s（含数据查询 + Canvas 绘制） | 🔲 需手动验证 |
| 9b-4 | 快速操作防抖 | 快速切换粒度、快速点击保存（防重复提交）、快速滚动不崩溃 | ✅ 完成 |

### 9c：边界测试 + 进程恢复 ✅ 完成

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 9c-1 | 极端金额 | 0.01 元、9,999,999.99 元显示不截断不错位 | ✅ 代码防御通过 |
| 9c-2 | 跨年数据 | 2024→2025 范围查询、年视图切换不丢数据 | ✅ 代码防御通过 |
| 9c-3 | 大量分类 | 50+ 一级分类，拖拽排序不卡顿 | 🔲 需手动验证 |
| 9c-4 | 空数据库首次启动 | 种子数据播种正常，首页不崩溃 | ✅ 代码防御通过 |
| 9c-5 | 配置变更 | 横竖屏切换、深色模式切换不崩溃 | 🔲 需手动验证 |
| 9c-6 | 进程被杀恢复 | ViewModel + SavedStateHandle 关键状态保持 | 🔲 需手动验证 |

### 9d：代码清理 + 手动验收 ✅ 完成

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 9d-1 | WheelPicker 副本清理 | 统计页 `WheelPickerForStat` 已删除，复用公共 `WheelPicker` | ✅ 完成 |
| 9d-2 | 废弃代码清理 | `FlowTypeRepository` 为预留接口（注释清晰），无废弃代码 | ✅ 完成 |
| 9d-3 | TODO/FIXME 处理 | 代码中无 TODO/FIXME 标记 | ✅ 完成 |
| 9d-4 | 冷启动白屏 | 主题添加 windowBackground 品牌色，新增 StrictMode 调试检测 | ✅ 完成 |
| 9d-5 | V1.0 手动验收 checklist | 创建验收清单（84 项），见 `V1.0验收清单.md` | ✅ 完成 |

> **审阅意见（2026-06-17）**：原"过渡动画"降级为"如有闪烁则修复"；"触觉反馈"移除（记账场景无实质收益）；新增首次使用引导、快速操作防抖、进程恢复、Migration 正式化、代码清理、冷启动白屏。

---

## 页面依赖关系

```
阶段 2: 首页 (A) ──→ 记一笔 (B)
    │
    ├──→ 阶段 3: 流水列表 (D) ──→ 详情页
    │
    ├──→ 阶段 4: 统计页 (C)
    │         ├── 构成分析
    │         └── 趋势分析
    │
    ├──→ 阶段 5: 预算设置 (F)
    │
    └──→ 阶段 6: 分类管理 (E)

阶段 8: 设置页 ──→ CSV导入导出 + 数据管理
阶段 9: 收尾优化
阶段 10: V1.0.1 体验优化
阶段 11: 多套主题 + 自动记账
    ├── 视觉线 (11a-11e): 预设主题 → 颜色选择器 → 分类颜色 + 图表配色
    │     └── 影响：所有现有页面（通过主题层）
    └── 自动记账线 (11f-11h): 数据层 → 调度引擎 → 模板管理页
          └── 影响：流水列表（🔄标记、编辑/删除差异）、设置页（新入口）
```

---

## 当前进度

| 指标 | 数值 |
|---|---|
| 已完成阶段 | 0、1、2、3、4、5、6、8、9、10 |
| 开发中阶段 | — |
| 待开发阶段 | 11（V1.1） |
| 已推迟阶段 | 7（并入阶段 11） |
| 已完成文件数 | ~73 个 Kotlin 文件 |
| 核心能力 | 首页数据聚合 + 记一笔（连续记账模式）+ 流水列表 + 详情页 + 统计页 + 预算 + 分类管理 + 设置页 + CSV导入导出 + 数据管理 + 防抖 + 数据库异常兜底 + 冷启动优化 + 体验优化（V1.0.1） |
| **V1.0.1 发布状态** | **✅ 就绪（2026-06-21）** |

---

## 阶段 10：V1.0.1 体验优化

**状态**：✅ 完成（2026-06-21）

> 需求来源：`docs/2-需求规格/requirements-v1x.md` 第 2 章。
> 用户 V1.0 实际使用后的体验反馈，不涉及新功能模块。

### 10a：分类管理交互优化

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 10a-1 | 展开热区扩大 | 整行可点击展开/折叠（当前仅文字可点击） | ✅ 完成（会话 #36） |
| 10a-2 | ~~二级分类 ▲▼ 排序~~ | ~~用上下移动按钮替代拖拽~~ → 体验不佳，已移除（会话 #38）；一级保留长按拖拽 | ❌ 已移除 |
| 10a-3 | ~~视觉层次优化~~ | ~~一级加颜色竖条~~ → 视觉效果不佳，已移除（会话 #38） | ❌ 已移除 |
| 10a-4 | 批量删除功能 | 批量模式多选一级分类，统一确认弹窗（保留/删除记录） | ✅ 完成（会话 #39） |

### 10b：统计页时间选择器统一

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 10b-1 | 滚轮式选择器 | 统计页时间选择改为 YearMonthWheelSheet 滚轮式，与流水列表一致 | ✅ 完成（会话 #35） |
| 10b-2 | 年月合并面板 | 年/月合并为一个滚轮面板，而非两个独立对话框 | ✅ 完成（2026-06-21） |

### 10c：UI 风格优化

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 10c-1 | 默认风格打磨 | 全局配色、圆角、间距、阴影等视觉优化 | ✅ 完成（会话 #40） |
| 10c-2 | 多主题架构预留 | 代码层面预留主题切换能力，为 V1.1 多套预设主题做准备 | ✅ 完成（会话 #40） |

### 10d：数据可视化增强

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 10d-1 | 分类颜色映射 | 为各分类分配固定颜色（餐饮=橙、交通=蓝等），首页和统计页统一 | ✅ 完成（会话 #33） |
| 10d-2 | 排行颜色 | 本月支出 TOP5 按分类显示不同颜色 | ✅ 完成（会话 #33） |
| 10d-3 | 条形图/饼图优化 | 显示金额 + 占比%，颜色与分类对应 | ✅ 完成（会话 #40） |
| 10d-4 | 趋势图评估 | 评估是否增加预算参考线、动画过渡等 | ✅ 完成（会话 #40） |

### 当前进度

| 指标 | 数值 |
|---|---|
| 已完成子阶段 | 10a-1、10a-4、10b-1、10b-2、10c-1、10c-2、10d-1、10d-2、10d-3、10d-4 |
| 已移除子阶段 | 10a-2（二级▲▼）、10a-3（颜色竖条）— 体验/视觉不佳，会话 #38 移除 |
| 开发中子阶段 | — |
| 待开发子阶段 | —（阶段 10 全部完成 ✅） |

---

## 阶段 11：V1.1 多套主题风格 + 固定收支自动记账

**状态**：✅ 已完成（2026-06-21）

> 需求来源：`docs/2-需求规格/requirements-v1x.md` 第 3 章。
> 核心交付物：5 套预设主题 + 自定义颜色体系 + 固定收支自动记账。

### 子阶段依赖关系

```
11a: 视觉基础设施 ──→ 11b: 主题切换入口 ──→ 11c: 颜色选择器组件 ──┬──→ 11d: 自定义分类颜色
                                                              │
                                                              └──→ 11e: 自定义图表配色
                                                              
11f: 自动记账数据层 ──→ 11g: 自动记账引擎+联动 ──→ 11h: 模板管理页

视觉线（11a-11e）与自动记账线（11f-11h）无功能依赖，可交叉或并行推进。
```

### 11a：视觉基础设施

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11a-1 | 硬编码颜色修复 | ~~`TransactionDetailScreen.kt` 5 处硬编码 → 替换为 `AppColors` 变量~~ V1.0.1 已验证，无硬编码 | ✅ 已就绪 |
| 11a-2 | DataStore 键定义 | 新增 `UserPreferencesRepository`（DataStore 封装）：`theme_preset_name`（String，默认"默认紫"）+ `custom_chart_palette`（JSON String，空=跟随主题），通过 Hilt `@Singleton` 注入；`libs.versions.toml` 添加 `datastore-preferences` 依赖 | ✅ |
| 11a-3 | 预设色值定义 | 新建 `ThemePresets.kt`，定义 5 套预设（默认紫/清新绿/深邃蓝/暖橙/极简灰），每套含 `lightColorScheme` + `darkColorScheme` + `ChartColorScheme` | ✅ |
| 11a-4 | 清理死代码 | 移除 `LocalDarkThemeOverride`（已定义但 `MyPurseTheme` 未消费，暗黑模式跟随系统后无用途） | ✅ |

**技术约束**：
- 色值用 Material3 的 `lightColorScheme()` / `darkColorScheme()` 构建，确保所有语义 slot 正确填充
- chartColors 的 palette/paletteExtended 数组长度 ≥ 6 和 ≥ 10，兜底色保留
- AppColors 功能语义色（`AppExpenseRed` 等）不随预设变化

### 11b：主题切换入口

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11b-1 | 设置页入口启用 | 将「主题风格」从灰显改为可点击，显示当前预设名 + 小型颜色点预览 | ✅ |
| 11b-2 | 预设选择 BottomSheet | 展示 5 套预设列表（预设名 + 颜色预览环 + 当前选中标记），点击切换即时生效 | ✅ |
| 11b-3 | 明暗跟随系统 | 确认 `MyPurseTheme` 正确读取系统的浅色/深色状态并自动切换预设的 lightScheme/darkScheme | ✅ 已有代码满足 |

**当前基础**：`ThemePreset` 数据类、`LocalThemePreset`、`MyPurseTheme(preset)` 签名已就绪（阶段 10c-2），无需改架构。

### 11c：颜色选择器组件（公共）

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11c-1 | HSV 色相环 Canvas 绘制 | 色相环（角度→色相，半径→饱和度）+ 明度滑块 + 预览色块 | ✅ |
| 11c-2 | 预设色盘 | 12 个推荐色块快速选择，「自定义」按钮跳转 HSV 取色 | ✅ |
| 11c-3 | 交互手势 | 色相环拖拽 + 明度滑块拖拽 + 实时预览 | ✅ |

**技术选型**：Canvas 自绘，零依赖。供 11d（分类颜色）和 11e（图表配色）复用。

### 11d：自定义分类颜色

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11d-1 | 分类管理页颜色入口 | 一级/二级分类行新增颜色圆点 + 「颜色」按钮 → 弹出 ColorPickerSheet | ✅ |
| 11d-2 | 内置分类「恢复默认」 | ColorPickerSheet 底部「恢复默认颜色」按钮（仅内置分类显示），将 color 还原为 CategoryDefaults 值 | ✅ |
| 11d-3 | 新建分类颜色可选 | CategoryEditDialog 新建模式下显示颜色预览提示；Repository 层 addCategory 支持 color 参数，color=0 时自动取 rotationColors 轮转色 | ✅ |
| 11d-4 | 全局统一取色源确认 | 首页 TOP5 ✅ / 统计页 ✅ / 分类管理页 ✅ — 均从 category.color 取值 + chartPalette fallback；记一笔/流水列表不涉及分类颜色展示，正常 | ✅ |

### 11e：自定义图表配色 → ❌ 已移除

> **2026-06-21 移除**：每个分类在 11d 已有固定颜色，自定义调色板只在分类 color=0 时作为 fallback 生效——而内置分类全有色、新自定义分类自动配色，fallback 几乎不触发。功能实际无可见效果，移除后减少设置页复杂度。同时新增 `backfillCustomCategoryColors()` 为旧自定义分类（color=0）一次性补色，确保所有分类都有稳定颜色。

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11e-1 | 设置页入口启用 | ~~「统计图配色」项~~ → 已移除 | ❌ 移除 |
| 11e-2 | palette 序列编辑器 | ~~ChartPaletteEditorSheet~~ → 文件已删除 | ❌ 移除 |
| 11e-3 | JSON 序列化/反序列化 | ~~DataStore 键 `custom_chart_palette`~~ → 已移除 | ❌ 移除 |
| 11e-4 | 图表覆盖逻辑 | ~~LocalMergedChartPalette + CompositionLocalProvider~~ → 已移除 | ❌ 移除 |
| 11e-5 | 旧自定义分类颜色回填 | `DatabaseInitializer.backfillCustomCategoryColors()`：isDefault=false 且 color=0 的分类按 flowSign 分组、按 id 排序、8 色轮转分配 | ✅ 新增 |

### 11f：自动记账数据层审查对齐

> **现状**：`recurring_template` 表（MIGRATION_3_4 已创建）、`RecurringTemplateEntity`（12 字段）、`RecurringTemplateDao`（7 方法，含 `getActiveTemplates()`）均已存在。本节任务是审查现有实现，按 V1.1 需求对齐。

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11f-1 | Entity 审查 | 现有字段对照需求 3.2.2 逐项比对：`cycleValue`（Long，SQLite `cycle_value`）、`isActive`（Boolean，SQLite `is_active`）——保持列名不动，Kotlin 属性名通过 `@ColumnInfo` 映射。无需 Migration | ✅ |
| 11f-2 | DAO 方法审查 | 现有 7 方法已覆盖 CRUD + `getActiveTemplates()` + `deleteAllTemplates()`。确认是否需要补充 `getByDateRange()` 等查询 | ✅ |
| 11f-3 | Repository 层补建 | 新建 `RecurringTemplateRepository`（领域接口）+ `RecurringTemplateRepositoryImpl`（实现），`AppModule` 绑定 | ✅ |

### 11g：自动记账引擎 + 联动

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11g-1 | RecurringScheduler | 独立类，冷启动时协程调用：查询启用模板 → 计算应执行日期 → 防重复 → 创建记录 → 更新 lastExecutedDate/nextExecutionDate | ✅ |
| 11g-2 | 补记策略 | 最多补记过去 90 天遗漏；无效日期退到当月最后一天 | ✅ |
| 11g-3 | 启动集成 | 在 AppEntry LaunchedEffect 中协程调用，不阻塞 UI 渲染 | ✅ |
| 11g-4 | 流水列表 🔄 标记 | `recurringTemplateId != null` 的记录在列表行显示循环图标（已在阶段 8 实现） | ✅ |
| 11g-5 | 编辑自动记录 | 编辑页面显示模板关联警告；保存后 `recurringTemplateId` 置 null（已在阶段 8 实现，删除确认文案已对齐需求） | ✅ |
| 11g-6 | 删除自动记录 | 弹窗提示"此记录由固定模板自动生成，删除后模板仍会在下次到期时生成新记录。"（已对齐需求文案） | ✅ |
| 11g-7 | 清除数据联动 | `clearAllData` 已调用 `deleteAllTemplates()`，确认兼容 | ✅ |

### 11h：模板管理页（新页面 G）

| # | 事项 | 说明 | 状态 |
|---|---|---|---|
| 11h-1 | 模板列表页 | 所有模板列表：周期标签 + 分类名 + 金额 + 下次执行日期 + 启用/禁用 Switch | ✅ |
| 11h-2 | 新建模板表单 | 选择流水类型 → 分类 → 金额 → 周期类型 → 周期参数 → 可选备注 → 保存 | ✅ |
| 11h-3 | 编辑/删除模板 | 点击进入编辑、删除确认弹窗："删除此模板，已生成的记录不受影响。确定删除？" | ✅ |
| 11h-4 | 空状态 | "暂无固定收支模板" + 新建按钮 | ✅ |
| 11h-5 | 设置页入口恢复 | 「固定收支模板」项在记账设置分组中，跳转模板列表页 | ✅ |
| 11h-6 | 导航注册 | `MyPurseNavHost` 添加 TemplateList + TemplateEdit 路由 | ✅ |
| 11h-7 | 收入分类选择 Bug 修复 | `FlowTypeOption` 新增 `sign` 属性，`loadForCreate`/`loadForEdit`/`onFlowTypeChanged` 三处硬编码 `-1` 修正为动态 sign | ✅ |

### 11 阶段新增/修改文件预估

| 分类 | 文件 | 操作 |
|---|---|---|
| **主题** | `ui/theme/ThemePresets.kt` | 新建 — 5 套预设色值 |
| **主题** | `ui/theme/Theme.kt` | 修改 — MyPurseTheme 读取 DataStore 预设 |
| **组件** | `ui/components/ColorPickerSheet.kt` | 新建 — HSV 色相环 + 色盘 |
| **组件** | `ui/components/` 目录 | 修改 — 如有其他公共组件调整 |
| **分类** | `ui/category/CategoryManageScreen.kt` | 修改 — 新增颜色入口 |
| **分类** | `ui/category/CategoryEditSheet.kt` | 修改 — 新建流程可选颜色步骤 |
| **设置** | `ui/settings/SettingsScreen.kt` | 修改 — 启用主题/图表配色/模板入口 |
| **设置** | `ui/settings/SettingsViewModel.kt` | 修改 — DataStore 读取/写入 preset 和 palette |
| **数据** | `data/local/entity/RecurringTemplateEntity.kt` | 审查 — 已存在，按需求对齐字段命名映射 |
| **数据** | `data/local/dao/RecurringTemplateDao.kt` | 审查 — 已存在，确认方法是否满足需求 |
| **数据** | `data/local/database/AppDatabase.kt` | 修改 — 已有 `recurringTemplateDao()`，无需 Migration（表已存在） |
| **数据** | `domain/repository/RecurringTemplateRepository.kt` | 新建 — 领域接口 |
| **数据** | `data/repository/RecurringTemplateRepositoryImpl.kt` | 新建 — 实现 |
| **引擎** | `data/local/RecurringScheduler.kt` | 新建 |
| **页面** | `ui/template/TemplateListScreen.kt` | 新建 — 模板列表 |
| **页面** | `ui/template/TemplateEditScreen.kt` | 新建 — 新建/编辑模板 |
| **页面** | `ui/template/TemplateViewModel.kt` | 新建 |
| **导航** | `ui/navigation/MyPurseNavHost.kt` | 修改 — 新路由 |
| **DI** | `di/DatabaseModule.kt` | 审查 — DAO 绑定可能已存在，确认 |
| **DI** | `di/RepositoryModule.kt` | 修改 — 新增 Repository 绑定 |
| **联动** | `ui/transactionlist/TransactionListScreen.kt` | 修改 — 🔄 标记 + 编辑/删除差异 |
| **联动** | `ui/detail/TransactionDetailScreen.kt` | 修改 — 编辑提示 + 硬编码→AppColors |
