# MyPurse 更新日志

## V1.3（2026-07-01）— 首页增强 + 统计修复 + 快捷退款

### 首页增强
- 概览卡片隐私保护：默认隐藏金额（`****`），眼睛图标切换显示/隐藏（冷启动重置）
- 概览卡片内容调整：从"本月支出/收入"改为"总支出/总收入"，避免与下方汇总行重复

### 统计页
- 环形图比例计算修复：子分类百分比与父分类进度条对齐
- 二级展开交互保留（V1.4 将重构为单分类详情页）

### 快捷退款
- 编辑页支出记录底部显示「退款」按钮，点击跳转收入记账页
- 自动预选「退款」分类（若用户已删除则自动恢复）、预填金额、备注为原记录日期
- 支持部分退款（可修改金额）

### 其他
- 批量修改分类：分析后取消（"迁移后保留空壳"无实际价值）

---

## V1.2（2026-06-24）— 分类管理增强 + 流水分页简化

### 分类管理增强
- 分类改名同步快照（方案B）：改名或合并分类后，自动批量更新历史流水中的分类快照列，确保全页面显示一致的新名
- 合并分类迁移：删除一级分类时可选"合并到其他分类"，自动迁移所有关联流水 + 子分类归入目标，删除源分类
- 方案A→方案B重构：从"读时 JOIN 反查"改为"写时同步快照"，删除 `categoryNameById`/`combine`/`toDomain(nameMap)` 等冗余逻辑，数据一致性更好

### 流水列表
- 移除分页：删除 `currentOffset`/`pageSize`/`accumulatedList`/`loadMore()` 及底部加载 spinner，所有视图改为一次性全量加载
- 各视图独立上限：日 500 / 周 1000 / 月 3000 / 分类 3000 / 年 10000。个人记账单一视图数据量远不超出这些值

### 数据层
- DAO 聚合查询从 `LEFT JOIN + COALESCE` 改为 `JOIN + cd.name`，消除 SQLite 非严格模式下的不确定性
- DAO 新增 `updateCategoryL1Snapshot` / `updateCategoryL2Snapshot` 批量快照更新方法

### Bug 修复
- 修复年视图部分月份不显示（`hasMore` 变量用错）
- 修复分类模式→时间模式切换闪退（`setGroupMode()` 未清除 `categoryL1Id`）

---

## V1.1.1（2026-06-22）— 体验修复

### 首页
- 移除首页标题"本月概览"（不准确）
- splash 等待 RecurringScheduler 补记完成后消失，避免首页数字闪变

### 固定收支模板
- 月/年周期 day 输入超界无提示 → 输入框下方加灰色提示文字
- RecurringScheduler 逐条写入改为先收集、再批量 insertTransactions（单事务）、最后更新模板

### 流水列表
- 流水分页统一：日/周/月/年所有视图统一 pageSize + accumulatedList 累积分页

---

## V1.1（2026-06-21）— 多套主题风格 + 固定收支自动记账

### 主题体系
- 5 套预设主题（默认紫/清新绿/深邃蓝/暖橙/极简灰），每套自带浅色+深色配色，明暗跟随手机系统
- 自定义分类颜色：分类管理页可修改任意分类颜色，全局统一取色
- 自定义图表配色：12 色色盘预设 + HSV 色相环取色器

### 固定收支自动记账
- 支持每天/每周/每月/每年周期模板，冷启动时自动检查并补记（最多 90 天）
- 模板管理页：列表展示 + 新建/编辑/删除模板 + 启用禁用开关
- 与流水联动：自动记录带 🔄 标记，编辑后解除模板关联，删除时特殊提示

---

## V1.0.1（2026-06-21）— 体验优化

### 分类管理
- 展开热区扩大：整行可点击展开/折叠二级分类
- 新增批量删除功能：批量模式多选一级分类，确认弹窗二选一（保留/删除关联记录）
- 移除二级分类 ▲▼ 排序按钮（体验不佳）
- 移除一级分类颜色竖条（视觉效果不佳）

### 统计页
- 时间选择器全量统一为滚轮式（YearMonthWheelSheet），与流水列表一致
- 年月合并面板：年/月合并为一个滚轮面板，而非两个独立对话框
- 构成分析 CompositionBar 文字颜色修复：文字从条形上方改为右侧排列，使用 MaterialTheme 自动适配明暗
- 趋势图移除预算参考线（橙色虚线），与需求规格对齐
- 分类颜色体系统一：各分类固定颜色映射（餐饮=橙、交通=蓝等），首页和统计页共用

### 首页
- 趋势图金额从始终显示改为点击柱子显示（按需揭示模式），避免数字拥挤

### UI 风格
- 全局配色、圆角、间距、阴影打磨
- 多主题架构预留（ThemePreset 体系），为 V1.1 多套预设主题做准备

### 数据层修复
- SQL NULL 比较语法修复：`IS :param` → `= :param OR (IS NULL AND :param IS NULL)`
- BudgetViewModel 架构违规修复：直接注入 DAO → 注入 Repository
- TransactionListViewModel 硬编码 `Int.MAX_VALUE` → `MAX_LOAD_SIZE = 500`

---

## V1.0（2026-06-20）— 首次发布

- 记一笔：自定义数字键盘、两级分类选择器（支出/收入分离）、连续记账模式、日期选择
- 首页：结余概览、预算进度条、近6月趋势柱状图、TOP5 排行
- 流水列表：日/周/月/年分组、分类筛选、搜索、左滑删除/修改
- 统计页：构成分析（条形图+饼图）、趋势分析（柱状+折线图）
- 分类管理：两级分类 CRUD、拖拽排序、恢复默认
- 预算设置：金额输入 + 快捷预设
- 设置页：CSV 导入/导出、清除所有数据
- 纯本地存储，零权限申请
